# Stakeholder Announcement Engine — Build Checklist

Tracks execution of `docs/EventPilot-SAE-PRD-v1.0.md` (currently v1.4). Update checkboxes as items land — this is the source of truth for "what's done" across sessions, alongside `HANDOFF.md`.

**Architecture decisions locked in (differ from the PRD document's literal text — see Context in the plan file, `~/.claude/plans/magical-singing-rossum.md`):**
- Extend existing `event_speakers`/`event_sponsors` additively — do **not** create separate `event_partners`/`stakeholder_speakers` tables. `event_speakers`/`event_sponsors` already exist for Website Builder + KonfHub; confirmed live-but-empty (0 rows) before this decision.
- New API routes live under `app/api/events/stakeholders/*`, not flat under `app/api/events/*` — avoids colliding with the existing `app/api/events/speakers`/`sponsors` routes.
- Public assets (creatives, photos, logos, messaging PDFs) go in a public Supabase Storage bucket (`event-stakeholder-assets`, `app/lib/events/storage.ts`), not R2 — matches Website Builder's existing pattern, gives stable non-expiring URLs.
- PhotoRoom (not remove.bg), plain pasted URL (not Google Maps API) — per PRD v1.2, unchanged since.
- **Postiz Cloud** (platform.postiz.com, managed SaaS, $39/mo Team plan) — not self-hosted. PRD v1.1/v1.2 originally called for self-hosted Postiz on Railway; v1.3 (2026-07-21) replaced that with the Cloud plan before Phase E was ever started, so no Railway deployment work happens for this module at all. `POSTIZ_API_URL=https://api.postiz.com` + `POSTIZ_API_KEY` (global) + per-event `postiz_profile_key` (workspace key).
- **Sharp compositing, not Canva Autofill** (PRD v1.4, 2026-07-21) — see the superseded Phase C section below for why. Canva OAuth stays in the codebase for future use, just not called during creative generation.
- **Named creative variants, each an ordered layer stack** — not a single background + fixed zones (PRD v1.4 Phase C v3, 2026-07-21, same day). Real creatives have genuine z-order between elements (e.g. a photo sitting under a translucent foreground for a feathered blend); see the Phase C v3 section below.
- **`postiz_profile_key` and `creative_template_config` stay per-event, not moved to a higher "Event Series/brand" level** — considered and explicitly rejected 2026-07-21. Brainstormed grouping recurring event brands (World AI Show, AJMS CXO Boardroom, etc.) into a series so their social channels/templates get configured once and reused across editions; Madhu's call was that not every event belongs to a recurring series, and standalone one-off events still need their own keys — so per-event stays the rule, no new `event_series` concept. This already matches what's built: `postiz_profile_key` sits in the same event-edit-form section as the `social_linkedin`/`social_x`/etc. fields (`app/admin/events/[id]/page.tsx`), so whoever updates an event's social channels updates its Postiz key in the same place. If a real duplication-effort problem shows up in practice across many editions of the same brand, revisit this — the reasoning here was about correctness for one-offs, not a verdict that series-level config could never be useful.
- **Speaker photo alignment via Gemini, logo PDF/AI conversion via `pdfjs-dist`+`@napi-rs/canvas`, not Google Cloud Vision / a paid conversion API** (PRD v1.4 Phase C v4, 2026-07-26/27) — both confirmed via real spikes to work reliably with zero new subscriptions, given Madhu's explicit cost-consciousness about the growing list of external EventPilot subscriptions. See the Phase C v4 section below.
- **Speaker Photo Engine and Company Logo Engine live outside the Announcement Engine** (`app/lib/media/`) — a processed photo/logo is a reusable asset (Website Builder, future consumers), not something specific to one creative. Scoped to SAE only for this build; Website Builder's own existing speaker/logo handling is untouched — migrating it is a separate, deliberate task if it's ever worth doing.

---

## Phase A — Foundation ✅ (committed, corrected for v1.2)

- [x] A1: `supabase/sae_migration.sql` written and applied to live Supabase
- [x] A2: Event profile edit form extended (Digital Presence / Social Channels sections), `Event` type, `app/api/events/route.ts` select list
- [x] A3 (superseded by v1.2 §6.2):~~Venue map search~~ → plain `venue_map_url` text field + "View Map ↗" link — done
- [x] A4: Topline Messaging Doc card, upload + Gemini structured extraction, `app/api/events/stakeholders/messaging/{route,[id]/route}.ts`
- [x] PRD v1.2 correction pass: Ayrshare → Postiz naming, Google Maps route deleted, DB columns renamed live

**Outstanding from Phase A:**
- [ ] **Lint debt**: `app/admin/events/[id]/page.tsx` carries ~82 pre-existing raw-hex-color errors unrelated to SAE (predates this work). Fix in one dedicated pass before this branch can pass `npm run lint:changed` in CI. Not urgent — do this right before opening a PR, not mid-feature-build.
- [ ] Authenticated click-through of Phase A (edit form save/display, messaging doc upload) — blocked on having a working local login; do this once Phase B gives us a real reason to run the dev server end-to-end.

---

## Phase B — Stakeholder Registry ✅ B1–B4 committed

- [x] B1: `app/api/events/stakeholders/speakers/route.ts` + `[id]/route.ts` (GET/POST/PATCH/DELETE on `event_speakers`, field-name mapping full_name/job_title/company_name ↔ name/role/company)
- [x] B1: `app/api/events/stakeholders/partners/route.ts` + `[id]/route.ts` (same, on `event_sponsors`)
- [x] B2: `app/api/events/stakeholders/speakers/[id]/upload-asset/route.ts` — photo/logo upload to Supabase Storage, PhotoRoom call for photos
- [x] B3: `app/api/events/stakeholders/partners/[id]/upload-asset/route.ts` — logo upload, PNG/JPG/SVG/PDF/AI
- [x] `from-submission` routes for both speakers and partners
- [x] Added: `app/api/events/stakeholders/submissions/{route,[id]/route}.ts` — list + reject, not in the PRD's original file list but required by its own §9.4 spec
- [x] Added: `'archived'` to both tables' `announcement_status` CHECK — PRD's own spec (§6.4) asks for it, never defined it
- [x] B4: `app/admin/events/[id]/stakeholders/page.tsx` — Stakeholder Hub. Registered in `app/lib/registry/modules.tsx` (`admin-event-stakeholders`, matching `admin-event-brief`'s pattern — `breadcrumbPattern` only, no `breadcrumbParent`, since this is a page-badge-only event-scoped entry like Plan/Execution/Brief, not a separately toolkit-gated tool like Website Builder/Market Intel). Renders `PageHeader`. `npm run check:nav` passes.
- [x] B5: `app/public/forms/[event_id]/[form_type]/page.tsx` + `app/api/public/forms/[event_id]/[form_type]/route.ts` — public, unauthenticated. Server-side required-field + file-size validation, honeypot field. Added `/public` to `middleware.ts`'s `PUBLIC_PREFIXES` (the page route wasn't covered by the existing `/api/public` entry — real gap, would have redirected every external submitter to `/login`) and to `AuthedShellGate.tsx`'s `PREFIX_NO_SHELL`. **Verified end-to-end for real**: unauthenticated `curl` submission with a real JPEG landed correctly in `stakeholder_form_submissions` with a working public photo URL (test row cleaned up after).

**Env var still needed to actually test B2/B3's PhotoRoom call:** `PHOTOROOM_API_KEY` (Madhu to get from photoroom.com/api) — code no-ops gracefully (stores the original photo, skips background removal) if unset, doesn't error.

---

## Phase C v1 — Canva Autofill ❌ ABANDONED 2026-07-21, superseded by Sharp (below)

Built and committed (C1/C2 below), then **investigated live with Madhu and confirmed non-viable**: the "Connect data" option needed to mark a Brand Template's elements as fillable does not appear on image or text elements in the standard Canva for Teams editor, even on genuinely-published Brand Templates. Confirmed two ways: (1) hands-on in Canva's editor, no such option found; (2) programmatically — got a real OAuth connection working end-to-end (scopes fixed, credentials verified, `GET /v1/brand-templates` returned both real WAIS Malaysia templates correctly), then `GET /v1/brand-templates/{id}/dataset` returned `{}` for both — zero fillable fields exist on either template. Canva's Autofill API requires a specific enterprise developer workflow to set up data fields that isn't reachable from the standard editor. This work is **not deleted from history** — kept here for the record — but the actual code has been removed from the branch (see Phase C v2).

- [x] C1: Added `'autofill'` to `app/api/canva/design/route.ts` + `runCanvaAutofill`/`pollCanvaJob` in `app/lib/canva.ts` — **removed** in the v2 rework; `getCanvaAccessToken` and the original five actions (upload/create/export/status/check) kept as-is, Canva OAuth stays for future use.
- [x] C2: `generate`/`regenerate-copy`/`regenerate-creative` routes built calling Canva autofill via `events.canva_template_config` — **reworked** in v2 to call Sharp instead.
- [x] Canva OAuth connection itself fully debugged and verified working (scopes, credentials, live token) — this groundwork is NOT wasted, it stays in place for whatever future use the OAuth integration gets.
- [x] Also fixed along the way: Railway auto-deploy had silently stopped since 2026-07-17 (payment lapse, unrelated to Canva) — found and resolved during this investigation.

---

## Phase C v2 — Sharp Compositing (single background + fixed zones) — superseded by v3, below

Built and verified end-to-end (C3 test below), then **superseded the same day**: looking at a real WAIS Malaysia creative, Madhu identified that real designs have independently-positioned elements with genuine z-order between them (e.g. a speaker photo sitting *under* a translucent foreground layer to get a feathered blend) — a single background PNG + fixed photo/logo/text zones can't express that. Not deleted from history — the Sharp-compositing *approach* (server-side, Canva stays the design tool for layer assets) carries forward unchanged into v3; only the config shape (fixed zones → an ordered layer stack) changed.

- [x] DB: `ALTER TABLE events RENAME COLUMN canva_template_config TO creative_template_config;` + `ALTER TABLE stakeholder_announcements DROP COLUMN IF EXISTS creative_canva_id;` — applied live and verified via `information_schema.columns`; `supabase/sae_migration.sql` updated to match with a header-comment history entry.
- [x] `npm install sharp` + `npm install react-image-crop` — both confirmed in `package.json` (`sharp@^0.35.3`, `react-image-crop@^11.1.2`).
- [x] Removed Canva Autofill code: `runCanvaAutofill`/`pollCanvaJob`/`CanvaAutofillField` deleted from `app/lib/canva.ts` (kept `getCanvaAccessToken` for the surviving 5 actions), `'autofill'` action removed from `app/api/canva/design/route.ts`, `buildAutofillFields`/`TemplateFieldMap`/`CanvaTemplateConfig` replaced by `buildCompositeInputs`/`CreativeTemplateConfig` in `app/lib/events/announcements.ts`.
- [x] New `app/lib/announcements/composite.ts` — `compositeAnnouncement()`, adapted from PRD §7 using real `uploadPublicAsset()` URLs (not the PRD sample's placeholder `r2:` scheme).
- [x] New `app/api/events/templates/upload/route.ts` (background PNG upload → Supabase Storage, `{r2_url}` response) + `app/api/events/templates/route.ts` (GET current `creative_template_config`).
- [x] Reworked `generate/route.ts` + `regenerate-creative/route.ts`: select `creative_template_config`, call `buildCompositeInputs()` → `compositeAnnouncement()` → `uploadPublicAsset()`, dropped `canva_staff_id` from both request bodies entirely, dropped `creative_canva_id`/`canva_edit_url` from insert/response.
- [x] Stakeholder Hub (`app/admin/events/[id]/stakeholders/page.tsx`): removed `canva_staff_id` from `generateAnnouncement()`'s body, removed the now-dead `staffId` state + its `/api/auth/session` mount effect.
- [x] New "Creative Templates" section in the event profile edit form (`app/admin/events/[id]/page.tsx`) — upload buttons for Speaker/Partner backgrounds (via `uploadTemplateBackground()`, auto-merges the returned URL into the right slot of the JSON draft) with preview thumbnails, plus a "Layout Config" `<textarea>` for the raw `creative_template_config` JSON with parse-error display (`saveEventEdit()` validates JSON before PATCHing). No visual coordinate-picker, per PRD §8's own scope call for the pilot.
- [x] Speaker photo crop/zoom tool: new `app/api/events/stakeholders/speakers/[id]/crop-photo/route.ts` (Sharp `.extract()` server-side against pixel coords, re-uploads, updates `photo_processed_url`) + new `app/admin/events/[id]/stakeholders/PhotoCropModal.tsx` (`react-image-crop` drag/resize UI, scales displayed-image coords to natural pixel dimensions before posting). Wired into `stakeholders/page.tsx`'s `uploadAsset()` — opens automatically after a speaker photo upload if `photo_processed_url` is set. **Speaker photos only — never partner logos**, confirmed.
- [x] C3: Manual test — **run for real** against the live dev server and live Supabase (not Madhu's real Canva export, a disposable synthetic one, since the goal here was verifying the compositing pipeline itself, not final creative approval): uploaded a solid-color 1080×1350 PNG via `/api/events/templates/upload`, set a real `creative_template_config` (photo_zone + name/title/company text layers) on a real event, seeded a disposable test speaker + photo, called `/api/events/stakeholders/announcements/generate` for real (201, real `creative_url`), downloaded and visually confirmed the composited PNG — photo positioned/sized correctly in its zone, all three text layers rendered in the right position/color/weight. All test rows (DB) and objects (Storage) deleted afterward. **Still blocked** on Madhu's real Canva-exported background PNGs + final layout coordinates for the actual production templates — this test only proves the pipeline works, not the final creative design.

---

## Phase C v3 — Multi-Layer Creative Editor (PRD v1.4, current)

`events.creative_template_config` now holds named **variants** per stakeholder type, each an ordered stack of **layers** (`image` / `photo_slot` / `text`), composited bottom-to-top. Photo/logo blending into a background (the feathered-edge case) is achieved purely by layer order + the uploaded PNG's own alpha channel — Sharp's `.composite()` already respects per-layer alpha, no custom masking code was needed. Four scope decisions confirmed with Madhu via AskUserQuestion (all "recommended" chosen): up/down-button reordering (not drag-and-drop), server-rendered live preview (not a client-side approximation), multiple named variants per stakeholder type, and alpha baked into the Canva export (not parametric in-app feather controls). Full architecture in `~/.claude/plans/magical-singing-rossum.md`.

- [x] DB: `ALTER TABLE stakeholder_announcements ADD COLUMN IF NOT EXISTS creative_variant_id TEXT;` — applied live, verified via `information_schema.columns`. `events.creative_template_config` itself needed no ALTER (still JSONB, only its internal shape changed); `supabase/sae_migration.sql` updated with a header-comment history entry + the new CREATE TABLE column.
- [x] `app/lib/announcements/composite.ts` — full rewrite: `Layer`/`Variant`/`CreativeTemplateConfig` types, canvas now starts blank (`sharp({create:...})`) and every layer (background art included) is one ordered `OverlayOptions` entry, so mixed z-ordering between images/photos/text is respected exactly (the old batched-SVG-at-the-end approach couldn't do this).
- [x] `app/lib/events/announcements.ts` — `buildCompositeInputs()` reworked to resolve a `Variant` (by `variant_id`, defaulting to the first configured), scan its layers for the distinct `photo_slot` sources actually referenced (not all three always required), and 422 with a clear message if a referenced source has no uploaded asset.
- [x] `generate/route.ts` + `regenerate-creative/route.ts`: accept optional `variant_id`, fetch buffers for every resolved source (was: one), store `creative_variant_id` on the row so regenerate can reuse it without re-picking.
- [x] Fixed a real bug surfaced by this rework: `templates/upload/route.ts` always wrote to a fixed `${type}-bg.png` path, silently overwriting any previous upload — fine when there was only ever one background, broken once a stakeholder type has many layer images across variants. Fixed with a unique path per upload; response key renamed `r2_url` → `url` (no external consumers).
- [x] New `app/api/events/templates/preview/route.ts` — runs the real Sharp pipeline against a **draft, unsaved** variant, returns a base64 data URL, nothing persisted. Uses a real speaker/partner's photo+text if `speaker_id`/`partner_id` is given, otherwise flat-color placeholder boxes + sample text, so the MM can preview before any real stakeholder data exists.
- [x] New `app/api/events/templates/variants/route.ts` — `PUT`, targeted update of just `creative_template_config.<type>.variants`, decoupled from the giant event-edit form's PATCH so saving speaker variants can never clobber partner variants.
- [x] New `app/admin/events/[id]/creative-templates/page.tsx` — full-page layer editor: speaker/partner tabs, variant list (add/rename/delete), per-variant layer list (add image/photo-slot/text, move up/down, inline property panel, delete), debounced (500ms) live preview pane with a "preview with" real-stakeholder selector. Registered in `app/lib/registry/modules.tsx` (`admin-event-creative-templates`, same shape as `admin-event-stakeholders`) — `npm run check:nav` passes clean, no baseline exemption needed.
- [x] Removed the v2 JSON-textarea/upload-button section from `app/admin/events/[id]/page.tsx`, replaced with a "Manage Creative Templates →" link to the new page — the dedicated editor now owns saving this data, the giant edit form doesn't compete for the same JSONB column.
- [x] Stakeholder Hub (`stakeholders/page.tsx`): fetches available variants per stakeholder type; shows a `<Select>` next to "Generate Announcement ▶" only when more than one variant exists for that type, passes the chosen `variant_id` through.
- [x] C3 test — **run for real**, extending the v2 test to actually prove z-order/blending (not just non-overlapping zones): built a 4-layer variant (background image → photo_slot → a second image layer with a semi-transparent band crossing the photo zone → name text), uploaded both image layers through `templates/upload` (confirmed the overwrite-bug fix — two distinct URLs), saved via `templates/variants`, tested `templates/preview` twice — once with a fully-opaque overlay (confirmed later layers correctly occlude the photo layer) and once with a 50%-alpha overlay (sampled pixels matched the exact expected alpha-blend math, e.g. `(197,69,202)` for 50% magenta over placeholder gray `(140,140,150)` — precise to the pixel). Then ran the full `generate` endpoint with a real speaker (201, `creative_variant_id` stored correctly, final composite visually confirmed: photo placed, foreground layer correctly drawn on top, name text rendered). All test DB rows and Storage objects deleted afterward.

**Still pending from Madhu (unchanged by this pivot):** Postiz Cloud workspace connect (LinkedIn/X/Instagram/YouTube OAuth — see Phase E). `PHOTOROOM_API_KEY` and `POSTIZ_API_KEY`/`POSTIZ_API_URL` **received and set in `.env.local`** (2026-07-21) — not yet on Railway, needs explicit go-ahead. Real Canva-exported layer images (backgrounds, feathered foregrounds) + final pixel coordinates for the actual WAIS Malaysia production templates — Madhu is reviewing the source designs' real layer structure to inform this.

~~**Known gap**: no browser-automation tool available this session~~ — resolved 2026-07-27, see the real Playwright click-through logged in the Phase C v4 section below (found Playwright already installed with its Chromium binary present). Variant/layer CRUD, move up/down, and Save all confirmed working in an actual browser.

---

## Phase C v4 — Photo/Logo Engines, Face-Aligned Compositing, Branding Platform (2026-07-26/27)

Madhu shared the real WAIS Malaysia production creative pre-split into its actual 6 layers, which surfaced real gaps between the Phase C v3 proof-of-concept and what a real creative needs: face-aware photo alignment (different speakers submit different shot types — headshot vs. shoulders vs. waist-level — that all need to line up consistently against one template), a from-scratch logo normalization pipeline (logos arrive in any format, with backgrounds that may or may not be part of the logo's own design), and a platform-level font library (text was hardcoded to a generic sans-serif). Worked through in extensive back-and-forth discussion (critique-then-answer rounds, not a single upfront spec) before any code — full reasoning trail in `~/.claude/plans/magical-singing-rossum.md`.

**Zero new paid vendors** — both of the two riskiest pieces turned out to have free/already-available solutions, confirmed via real spikes before committing to either:
- **Face detection**: Gemini (`gemini-2.5-flash`, structured `box_2d` output) instead of the originally-proposed Google Cloud Vision — reuses the existing `GEMINI_API_KEY`. Confirmed via a real test against a real reference photo before deciding.
- **PDF/AI logo rasterization**: `pdfjs-dist` + `@napi-rs/canvas` instead of a paid conversion API (CloudConvert) — both already present in `node_modules` as transitive deps of `pdf-parse`, promoted to direct dependencies. Confirmed Sharp itself has no PDF support compiled into this build's libvips first, then confirmed this free alternative works with a real test PDF, before committing.

- [x] **Font-embedding spike** (done first, de-risked everything font-related): fetched a real Google Font, embedded via base64 `@font-face` in an SVG, rendered through Sharp/librsvg — confirmed it actually uses the embedded font (visually distinct from a fallback-font control line) and correctly falls back for an unknown font-family rather than crashing.
- [x] **Platform-level font library**: new `brand_fonts` table + `app/admin/branding/fonts/page.tsx` (new standalone "Administration" section, not nested under any event) — upload TTF/OTF/WOFF/WOFF2 directly, or fetch any family by name from Google Fonts' free public API (`app/lib/branding/fonts.ts` parses the returned CSS for direct `.woff2` URLs, re-hosts our own copy). `TextLayer` gained an optional `font_family` (denormalized name + URLs, resolved into embedded `@font-face` CSS at render time in `composite.ts`); falls back to the generic sans-serif when absent, so every layer authored before this change keeps working unchanged. Font Family dropdown added to the Creative Templates editor's text layer panel.
- [x] **Company Logo Engine** (`app/lib/media/logo-engine.ts`) — any format (PNG/JPG/WebP/SVG/PDF/AI) → PDF/AI rasterized, SVG rasterized natively → background removal via a **border-touching flood-fill heuristic** (only removes pixels connected to the image border, so a logo's own legitimate colored badge/shield background survives) → canonical un-fit transparent PNG (safe-area fitting stays at use-time, via the existing `photo_slot` fit logic — no change needed there). Wired into both the partner and speaker's-own-company-logo upload routes (`logo_raw_url` keeps the untouched original, `logo_url` becomes the engine's output; falls back to the raw upload if processing errors). New `LogoApprovalModal` — approve or re-upload, no crop choice (the fitting math is fully deterministic). Tested against 5 real risk cases (plain wordmark on white, icon+wordmark, a logo with a legitimate colored badge — the highest-risk case, already-transparent PNG, SVG) — all behaved correctly, especially the badge case (survived intact while the surrounding white canvas was removed).
- [x] **Speaker Photo Engine + Face Alignment** (`app/lib/media/speaker-photo-engine.ts`, `app/lib/media/face-alignment.ts`) — generalized the inline PhotoRoom call into a reusable pipeline, added the confirmed "enhance" step (Sharp brightness/contrast normalize + sharpen). `deriveAlignmentTarget()` runs once per reference layer (alpha-trim for the box + Gemini for the target head position/size within it); `alignAndCropPhoto()` detects the real speaker's head, scales/positions to match, crops to fill — pads with transparency (doesn't re-scale) when the source photo lacks enough peripheral content, so a genuine shot-type mismatch stays visible rather than being silently papered over. Falls back to a simple contain-centered fit if no face is detected or anything fails. `PhotoSlotLayer` gained an optional `alignment` field; `composite.ts`'s photo_slot branch uses face-aligned-cover when present, unchanged contain-centered fit otherwise. **Two real bugs found and fixed via testing against actual photos** (not just a happy-path check): Sharp's `.trim()` reports offsets as negative (had the box position backwards); the original "scaled image too small for the box" fallback re-scaled the whole box, silently defeating the deliberate head-size match — fixed to pad at the same scale instead.
- [x] **Reference-driven photo_slot authoring**: new `POST /api/events/templates/derive-alignment` + an "Upload Reference Layer" option in the Creative Templates editor for speaker-photo slots — auto-fills the box and alignment target from an uploaded transparent PNG reference, replacing hand-typed pixel coordinates as the primary path (manual fields stay available). Stakeholder Hub's variant picker relabeled with a plain-language "Creative style" tooltip — confirmed design principle that MMs never see "layer/variant/safe area" vocabulary.
- [x] **Consolidated per-generation review screen**: new `AnnouncementReviewModal` shown right after a real `generate()` call — final composite, generated post copy, and thumbnails of the actual photo/logo used (not static layers, which never change per speaker), with Regenerate Creative / Regenerate Post Copy wired to the existing `regenerate-creative`/`regenerate-copy` endpoints. Replaces the old "Review UI is coming in Phase F" toast.
- [x] **Full end-to-end verification** combining every piece of this phase in one real test: uploaded a real speaker photo and a real colored-badge logo through the actual upload routes, derived a face-alignment target from a real reference photo, built a variant using all of it, called `generate` for real — the resulting composite correctly showed the face-aligned photo, the background-removed logo (badge intact), and both text layers, all correctly positioned. Confirmed `regenerate-creative`/`regenerate-copy` return exactly the shapes the review modal expects. All test rows/objects cleaned up afterward.

**Structural note carried over correctly**: the old `use_company_logo` swap (photo *or* logo in one slot) is gone — a variant now just includes a `speaker_logo` `photo_slot` layer or doesn't; `composite.ts` already skips missing optional sources gracefully, so photo and logo can coexist as independent layers (layer 2 and layer 5 in Madhu's real design) exactly as needed.

- [x] **Real browser click-through** (2026-07-27, follow-up session) — found Playwright already installed (`@playwright/test`) with its Chromium binary present, so this Phase C v3/v4 "known gap" got closed after all: drove a real headless-Chrome session through login, the Creative Templates editor (new variant → add all 3 layer types → expand the photo_slot → Upload Reference Layer → real Gemini call → correct auto-fill of box + "Face-aligned ✓" indicator → live preview → Save), the Font Library admin page (fetch Poppins from Google Fonts → appears in the library), the Stakeholder Hub's `LogoApprovalModal` (checkerboard correctly showed the badge survived background removal), and the full Generate → `AnnouncementReviewModal` flow (face-aligned photo, logo thumbnail, real Gemini post copy). **Found and fixed two real bugs this exposed that no amount of API-level testing would have caught**: (1) the variant auto-select effect only re-ran on tab *switch*, not on the initial data fetch, so reloading the editor with existing variants showed a misleading "no variants" empty state; (2) a JSX text/expression boundary was silently losing its adjacent space in the compiled output (confirmed via `JSON.stringify` on the real rendered text) — folded into one template literal to fix. All test data/objects created during the click-through cleaned up afterward.

**Still pending from Madhu:** real Canva-exported layer images (background, design-overlay, event-details) for the actual WAIS Malaysia production templates + final pixel coordinates/reference photos to derive real alignment targets from — this phase proves the pipeline works correctly, not that the final production creative has been built yet.

### Phase C v4.1 — Real-asset bug pass (2026-07-27, Madhu's own hands-on test)

Madhu tested the editor himself with the actual WAIS Malaysia production layer files (not synthetic test images) and reported a concrete bug list from real screenshots. Root cause for the three broken-preview reports turned out to be a single shared bug, confirmed by fixing it once and re-testing all three.

- [x] **Root cause**: `newLayer()` (`app/admin/events/[id]/creative-templates/page.tsx`) hardcoded new `image` layers to `400×400` regardless of the variant's actual canvas size (1080×1350) — a real full-bleed background/overlay upload got force-resized (`fit:'cover'`) into a 400×400 box, squishing it. This explains all three "uploaded fine but preview looks broken" reports (Layer 1 background, Layer 3 design element, and downstream the Layer 2 photo reference-layer preview which just sat on top of an already-squished background). **Fixed**: `newLayer()` now takes the active variant's `canvas_width`/`canvas_height` and defaults new image layers to that, matching how these layers are actually used in every real creative (full-canvas, pre-sized by the design team).
- [x] Removed the X/Y/Width/Height manual fields from `ImageLayerFields` entirely — image layers are always full-canvas by convention (confirmed by Madhu: "no one would actually be required to change it anyways"), so editable position/size fields for this layer type were pure surface area for a mistake. Row summary now shows `WxH (full canvas)` next to the upload button instead.
- [x] Fixed a related latent bug this surfaced: the debounced live-preview fires ~500ms after any layer edit, including immediately after "+ Image Layer" is clicked (before a file is chosen) — `compositeAnnouncement()` was calling `fetch('')` on the empty `asset_url` and throwing a 500. Now skips image layers with no `asset_url` yet, matching how `photo_slot` already skips missing optional sources.
- [x] **"Red N / 1 Issue" badge**: investigated via a cleared dev log + a dedicated fresh-navigation Playwright reproduction — did not reproduce on a clean load, and did not recur across 10+ further real navigations run later in this same session. One single hydration-mismatch line does appear at the very first cold request right after a dev-server restart (Turbopack's first on-demand compile), consistent with a one-off dev-mode timing artifact, not a persistent bug. Not chasing further; it's the same conclusion as before, now with much stronger evidence (zero recurrence across a long real test session).
- [x] **Delete Variant** — was instant, no confirmation, so a stray click destroyed a variant's full layer stack. Added a `window.confirm()` guard (this codebase's existing pattern, e.g. `courses/page.tsx`, `AssetsTab.tsx`) naming the variant and its layer count. Verified both paths live: Cancel preserves the variant, OK deletes it.
- [x] **Layout**: preview column widened from a fixed `340px` to an equal `1fr` share with the layer editor (was `220px 1fr 340px`, now `180px 1fr 1fr` — roughly doubles the live preview's real width on a normal screen). Dropped the redundant outer `Card` wrapping the layer editor (the individual `LayerRow` borders are now the only "box" in that column) so the page reads as one workspace instead of a card-in-a-card.
- [x] **Full real-asset live-browser verification** (Playwright, real login, real dev server) — reproduced Madhu's exact original bug (squished 400×400 preview) with his real Layer 1 bg.png and Layer 3 design-element.png files first, confirmed it, then re-ran after the fix: both now composite correctly full-canvas. Built a complete 6-layer speaker variant (image bg → photo_slot speaker photo with reference-layer auto-position, using his real Layer 2 file, Gemini detected "headshot" correctly → image design overlay → photo_slot speaker logo → text name → text title), saved it.
- [x] **Full public-form-to-creative end-to-end test**, per Madhu's explicit ask ("perform a dummy speaker form submission... ensure everything renders exactly as we intended"): submitted the real, unauthenticated public speaker onboarding form with dummy data + a real photo + a real logo file, processed the resulting submission into a real `event_speakers` row via the existing Stakeholder Hub "Process" action (confirmed the Speaker Photo Engine and Logo Engine both ran automatically — `photo_processed_url` and `company_logo_url` populated), then selected that real speaker in the editor's own Live Preview panel. Final render: full-canvas background, correctly face-aligned real photo, design overlay correctly blended on top, logo positioned, name/title text rendered — visually confirmed correct end to end.
- [x] Full verification suite re-run after every change: `npx tsc --noEmit`, targeted `eslint` on the two changed files, `npm run build`, `npm run check:nav` — all clean.

**Left in place for Madhu to review** (per his explicit request to leave live-test results in the system rather than clean them up): a disposable test speaker "Jane Doe / Acme Intelligence Corp" (clearly fake, `PENDING REVIEW`/`pending_review` status) on the real World AI Show - Malaysia 2026 event, and the "Speaker template 1" variant now populated with a real 6-layer stack built from Madhu's actual layer files — the speaker-logo box position (760, 1180, 260×130) was a hand-picked approximation for this test, not tuned against the real Layer 5 reference, so it will likely need repositioning once he reviews it. Playwright scripts and screenshots from every step of this session are in `.scratch/` (not committed).

**Separately observed, out of scope for this pass (not fixed — not asked for, and touches a different subsystem)**: the Stakeholder Hub's "Generate Announcement" button stays disabled until `announcement_status === 'ready'`, but nothing in the current UI ever sets a speaker to `'ready'` after form-submission processing (it lands on `'pending_review'` and stays there) — looks like a real gap in the not-yet-built Phase F approval-status wiring, not something introduced by this fix. Worth a look when Phase F is scoped.

### Phase C v4.2 — Second real-asset bug pass (2026-07-27, same day, Madhu reviewed the v4.1 result)

- [x] **Layer 4 (Event Details) was missing entirely** — v4.1's test build only used Layer 1/2/3 files, skipping Layer 4 because its cached copy (a mostly-white/light-gray "WORLD AI SHOW" graphic on a transparent PNG) looked ambiguous when viewed directly — **that was a viewing artifact, not the file's real content**: it's genuinely crisp white/bold event-detail typography ("48th Global Edition / WORLD AI SHOW MALAYSIA / 9-10 SEP 2026 / DoubleTree by Hilton Kuala Lumpur"), just invisible against a white preview canvas since the text itself is white. Confirmed by compositing it onto the real dark background — renders exactly right. Added as a 7th layer, positioned correctly in z-order (after the design-element overlay, before the logo/text layers) via 2 "Send backward" moves — matches Madhu's reference render.
- [x] **Logo positioning was wrong** — root cause: the reference-layer auto-position feature (added in this same session, see the commit above) didn't exist yet when the v4.1 logo box was set, so it was hand-typed and landed almost touching the hashtag/website text below it. Fixed by using the now-built logo reference-layer upload against Madhu's real `logo-placeholder.png` — derived box (739, 1123, 241×50) instead of the old guess (760, 1180, 260×130), correctly clear of the hashtag line, matching his reference render pixel-for-pixel in spirit.
- [x] Re-verified against the real "Jane Doe" test speaker's Live Preview — full match against Madhu's reference sample render (`Speaker-announcement-template.png`): background, event details, face-aligned photo, design overlay, correctly-positioned logo, name/title text all render as intended.
- [x] One transient hiccup noted, not a real bug: one preview render mid-session substituted a gray placeholder for the (real, reachable) logo URL — the preview route's own documented fallback for when `fetch(realUrl)` fails (`app/api/events/templates/preview/route.ts`). Didn't recur on a clean re-check immediately after; the URL was independently confirmed reachable (curl 200) throughout. Not chasing further absent a repro.
- [x] Full verification suite re-run: `npx tsc --noEmit`, `npm run build`, `npm run check:nav` — clean.

**MM access — investigated, not changed (a product/access decision, not a bug fix):** Madhu asked where MMs actually reach this to create real announcements. Findings: (1) the real public speaker onboarding form is `/public/forms/{event_id}/speaker` (unauthenticated, already built/tested in Phase B) — but **there is currently no in-app UI anywhere that surfaces or copies this link** for an MM to send out; someone has to know to construct the URL by hand. (2) The MM-facing surface for managing speakers/partners and clicking "Generate Announcement" is the Stakeholder Hub (`app/admin/events/[id]/stakeholders/page.tsx`) — **but its registry entry (`admin-event-stakeholders`, `app/lib/registry/modules.tsx`) is `access: { kind: 'admin_only' }` with no `grantKey`, identical to the Creative Templates editor**, so no non-admin role can reach it at all today. This wasn't a deliberate call for the Stakeholder Hub specifically (unlike Creative Templates, which the locked-in plan explicitly scoped admin-only for branding-team access) — it looks like it just inherited the same default while Phase F (the actual approval/role wiring) was still unbuilt. `app/lib/registry/access.ts` already has the machinery for a granular role (`dept_or_admin`, `role_or_dept_or_admin`, tool grants) whenever this gets scoped — not a dead end, just not decided yet. Left both open for Madhu/Durga to decide rather than unilaterally loosening access to internal admin tooling.

### Phase C v4.3 — Module split (landing + Admin Console) and real tiered access control (2026-07-27, same day)

Madhu asked three things in one message: (1) where to find the existing Speaker Photo Engine/Company Logo Engine tester (Stakeholder Hub → a speaker's own "Upload Photo"/"Upload Company Logo" buttons — already built, unchanged, just pointed him at it), (2) confirm the Font Library he'd asked about earlier is real and reachable (`/admin/branding/fonts`, Administration section — already built in Phase C v4, confirmed live via a real request, already wired into the SAE Text layer's Font Family dropdown), and (3) restructure the module: keep the base URL, move variant creation into a nested "Admin Console" (branding-team only for now), and add a real tiered Access Control section there — admin tier for branding team, standard tier for MMs, admin-granted.

- [x] **Split the single page into two routes**, same URL prefix as Madhu asked: `/admin/events/[id]/creative-templates` is now a lightweight landing page (links to the Admin Console + the Stakeholder Hub); the full layer-stack editor moved unchanged to `/admin/events/[id]/creative-templates/admin`.
- [x] **Reused the codebase's existing generic tiered-access system** rather than inventing a new one — `module_access` table (`tier: 'user' | 'admin'` per staff per module), the shared `<AccessTab>` component (already used by Commercial, Bespoke Tracker, KB, DocuHub, etc.), and the `/api/module-access/[moduleKey]/*` routes. New `moduleKey: 'sae'`. Registry (`app/lib/registry/modules.tsx`): the landing page is `admin_only` with `moduleAccessKey: 'sae'` (platform admin OR any granted tier gets in); a new `admin-event-creative-templates-admin` entry documents the Admin Console's intended `module_access` admin-tier requirement (for breadcrumbs — see below for why it isn't the actual enforcement path) and a `toolkitHub.access` override on the landing entry (`tool_grant`, `grantKey: null`, `moduleAccessKey: 'sae'`) so `'sae'` is a valid key for the generic `/api/module-access` routes at all (`getValidModuleAccessKeys()` only scans `tool_grant`-kind entries — a pre-existing convention, not something invented here).
- [x] **Real server-side enforcement**, matching the `website`/`brand`/`market-intel` pattern: `middleware.ts`'s `isToolRoute` regex extended to include `creative-templates` (was blanket-blocked to platform-admins-only before, since no layout existed and no tool-route exception covered it); new `app/admin/events/[id]/creative-templates/layout.tsx` (`requireModuleAccess('admin-event-creative-templates')`) gates the whole module; a stricter nested `app/admin/events/[id]/creative-templates/admin/layout.tsx` gates the console specifically.
- [x] **Real bug found and fixed while wiring the stricter gate**: the registry's generic `kind: 'module_access'` check (`checkAccess()` → `hasModuleAccess()`) has **no platform-admin bypass** — unlike `kind: 'admin_only'`, which checks `session.adm` first, `hasModuleAccess()` only bypasses for the literal `'super-admin'` sentinel or a `staff_members.job_level === 'super_admin'` row. Confirmed live: Durga's own admin login (`job_level: 'staff'`, despite `role: 'Platform Admin'` and `session.adm: true`) got redirected to `/no-access` by the Admin Console gate before this was caught. Fixed by hand-checking `session.adm OR hasModuleAccess(..., 'admin')` directly in `admin/layout.tsx` instead of routing through `requireModuleAccess()`'s generic `module_access` branch — documented inline so the next person doesn't reintroduce it by "simplifying" back to the generic path. Neither `dc@tresconglobal.com` nor `md@tresconglobal.com` (both real `job_level: 'super_admin'` in the DB) would hit this in production — it only affects the `reachcharan@gmail.com` local-dev password-login account.
- [x] **Second real bootstrap gap found and fixed**: even with the layout fix above letting Durga's admin login *into* the Admin Console, the shared `<AccessTab>` component's own self-check (`GET /api/module-access/sae/me`) has the *same* `job_level`-only bypass (no `session.adm` check) — a pre-existing characteristic of that shared, already-used-elsewhere route, not something new. Without an explicit `module_access` row, Durga's admin login would see "admin access required" *inside* the Access Control tab itself, unable to grant anyone. Fixed by seeding one real (non-disposable) `module_access` row — `{staff_id: reachcharan@gmail.com's id, module_key: 'sae', tier: 'admin'}` — the actual bootstrap grant, not test data; left in place.
- [x] **Breadcrumb gap found and fixed**: `deriveBreadcrumbs()`'s pattern-match branch only resolved a `breadcrumbParent` whose `href` was a plain string, and only walked one hop — both fine for every existing single-level event-scoped tool, but the Admin Console is the first *two-level* nested event-scoped page, and its immediate parent's `href` is a ctx function (needs `:eventId`). Extended `app/lib/nav/breadcrumbs.ts` to extract path params from the matched pattern and walk the *full* `breadcrumbParent` chain, resolving function-typed hrefs with those params. Confirmed live: breadcrumb now reads "My Dashboard › Toolkit › Stakeholder Announcement Engine › Admin Console" (was silently dropping straight to "My Dashboard › Admin Console").
- [x] **Full live verification** (Playwright, real login): landing page loads with both link cards; Admin Console loads with the existing 7-layer "Speaker template 1" variant fully intact (nothing lost in the file move); Access Control tab renders the real grant/revoke UI; granted a real staff member (Aaron Chow) `user` tier, confirmed it listed, revoked it (confirmed via a fresh `GET /api/module-access/sae` — Aaron's row gone, only the real Durga admin-tier bootstrap grant remains); confirmed an unauthenticated request to the module redirects to `/login`. Full verification suite clean: `npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`.
- [x] **Deliberately not built this pass** (not asked for): the actual MM-facing "pick a speaker, pick a variant, generate" flow doesn't live at the landing page yet — it stays in the Stakeholder Hub for now (linked from the landing page), and the Stakeholder Hub's own access is still `admin_only` with no tiered fallback (flagged in Phase C v4.2 above, unchanged here). A `user`-tier MM granted access via the new Access Control tab can reach the SAE landing page but still can't reach the Stakeholder Hub to actually generate anything — a real follow-up gap, not fixed silently.

### Phase C v4.4 — Font Library: Branding tab, live Google Fonts search, real drag-and-drop upload (2026-07-27, same day)

Three more asks in one message: (1) surface the Font Library from the Admin Dashboard the same way Events/HR Portal already are, (2) fix a real bug Madhu hit live — typing a font name in any case other than Google's exact canonical casing 400'd even though the font genuinely exists — by adding live search-as-you-type against Google's own catalog, matching fonts.google.com's own picker, and (3) replace the manual-family-name-plus-file-picker upload with real drag-and-drop where the font file's own metadata supplies the name, since (his words) "they'll type something random and it wont be searchable either."

- [x] **Admin Dashboard "Branding" tab** — `app/admin/page.tsx`'s tab bar gained a `branding` entry alongside Events/Finance/HR Portal, same nav-link-only pattern (`window.location.href` to `/admin/branding/fonts`, no inline content). Verified live.
- [x] **Root cause of the "not found (400)" bug**: Google's `css2` stylesheet API requires the *exact* canonical family-name casing ("Space Grotesk", not "space grotesk") — confirmed empirically via direct `curl` (same request, only casing differed, 400 vs 200). `fetchGoogleFontFiles()` never corrected for this.
- [x] **New `app/lib/branding/google-fonts-catalog.ts`** — the actual fix, plus the live-search feature in one piece of infrastructure: fetches and in-process-caches (12h TTL) the ~1,942-family catalog from `https://fonts.google.com/metadata/fonts` — the same public, **unauthenticated** endpoint fonts.google.com's own web app calls for its search box (not the official, API-key-gated Web Fonts Developer API — confirmed this unofficial one needs no key and no new credential from Madhu). `searchGoogleFonts(query)` powers the dropdown; `resolveCanonicalFamilyName(name)` case-corrects free-typed input before it ever reaches Google's css2 API.
- [x] New `GET /api/branding/fonts/search?q=` — thin wrapper over `searchGoogleFonts`. `fetchGoogleFontFiles()` and the `POST /api/branding/fonts` route both now resolve through `resolveCanonicalFamilyName()` before fetching *and* before storing, so the library's own display name is always the correct casing too, not whatever the user happened to type.
- [x] **Font Library page** (`app/admin/branding/fonts/page.tsx`) — "Add a Google Font" is now a real debounced (250ms) autocomplete: type, see live matches with category, click one to fill the input with the correct canonical name. Verified live (real request to Google's catalog): typing "ibm plex" returned 11 real matches (IBM Plex Mono/Sans/Sans Arabic/Sans Condensed/…), clicking one filled the input exactly. Re-verified the original bug directly: typing "league spartan" (lowercase) and hitting Fetch & Add — no longer 400s, added correctly as "League Spartan."
- [x] **New `fontkit` + `@types/fontkit` dependencies** — reads the family name and weight straight out of an uploaded font file's own OpenType `name`/`OS/2` tables (TTF/OTF/WOFF/WOFF2 alike). Confirmed adds zero new `npm audit` vulnerabilities (all 14 pre-existing platform-wide, unrelated — @babel/core, brace-expansion, drizzle-orm, esbuild/wrangler, js-yaml, next itself).
- [x] New `app/lib/branding/font-metadata.ts` (`parseFontMetadata()`) + new `POST /api/branding/fonts/bulk-upload` — accepts any number of font files in one request (individual drops or the whole brand kit at once), groups them by *detected* family name (never typed by hand), and per family: adds it if new, fills in a missing Bold weight if the family already exists with only Regular, or reports "already exists — skipped" if nothing new. Returns one result row per family for the UI to render as a notification list, so a batch upload's outcome is never a single opaque pass/fail.
- [x] **Real upload UI rework** — the old manual family-name text field + "Choose Regular + Bold Files" button is gone entirely (per Madhu: don't offer typing at all). Replaced with an actual `onDragOver`/`onDrop` zone (click-to-browse still works as a fallback) wired straight to the bulk-upload endpoint.
- [x] **Real race condition found and fixed via live testing** (not a hypothetical): an early test run's timing bug caused two overlapping bulk-upload requests for the same family, and both inserted successfully — the API's own "check existing, then insert" isn't atomic. Fixed at the database level, not just in application code: `CREATE UNIQUE INDEX brand_fonts_family_name_lower_idx ON brand_fonts (LOWER(family_name))`, applied live via `supabase db query --linked` (same pooler-DNS workaround noted elsewhere in `HANDOFF.md`), `supabase/sae_migration.sql` updated with a history entry. Both the bulk-upload route and the single-Google-Font route now catch Postgres `23505` (unique_violation) and report it as a normal "already exists" outcome (409 for the single route) instead of a raw 500.
- [x] **Removed now-dead code**: the old multipart family-name-typing branch of `POST /api/branding/fonts` — nothing calls it anymore now that bulk-upload fully supersedes it, and per Madhu's explicit ask, nothing should ever again offer typing a font name by hand.
- [x] **Full live verification**, clean end-to-end (all test data + orphaned Storage objects deleted afterward): dropped 4 real Google-sourced font files (Poppins Regular+Bold, Space Grotesk Regular+Bold) as one batch — both families added correctly with the right weights; re-dropped the same Poppins Regular file — correctly detected and skipped as a duplicate, exactly one Poppins/Space Grotesk row each in the final library.
- [x] Full verification suite: `npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav` — all clean throughout.

### Phase C v4.5 — "Generate Announcement does nothing" — the real missing status transition (2026-07-27, same day)

Madhu added a real speaker manually, clicked Generate Announcement, and nothing happened. Real bug, two layers to it:

- [x] **Root cause**: `announcement_status` starts `'pending_review'` for every speaker (manual add or onboarding-form conversion) and **nothing anywhere in the UI ever moved it to `'ready'`** — the exact value `Generate Announcement`'s `disabled={item.announcement_status !== 'ready'}` gate requires. This is the same gap flagged as a known follow-up in Phase C v4.1/v4.2 above ("looks like a real gap in the not-yet-built Phase F approval-status wiring") — now a real live blocker, not just an observation.
- [x] **Compounding visual bug**: `.tbtn` (`app/globals.css`) had **no `:disabled` styling at all** — a disabled button rendered pixel-identical to an enabled one (same solid color, same cursor), so there was no way to tell by looking that the button was inert. This is why it looked like a dead click rather than an obviously-disabled control. Fixed globally (`opacity: 0.4`, `cursor: not-allowed`, hover/active neutralized) — benefits every disabled button in the app, not just this one.
- [x] **Fix**: new "Approve for Announcement" action (`app/admin/events/[id]/stakeholders/page.tsx`) — PATCHes `announcement_status: 'ready'` via the existing `speakers/[id]`/`partners/[id]` route (already supported this field, just nothing called it). Shown whenever a stakeholder isn't already `ready`/`archived`, sitting right next to the now-correctly-greyed-out Generate Announcement button so the two-step "review, then generate" sequence is visible rather than a silent precondition.
- [x] **Full live verification** on Madhu's own real speaker ("Sir Alistair Raymond Pemberton"): confirmed Generate Announcement was genuinely `disabled` with the new dimmed styling (computed `opacity: 0.4`, `cursor: not-allowed`) before the fix; clicked Approve for Announcement, confirmed the badge flipped `PENDING REVIEW` → `READY` and the button became clickable; clicked Generate Announcement for real — 201, real composite (face-aligned photo, background-removed logo) and real Gemini-generated post copy rendered correctly in the review modal. Left this real announcement + the `ready` status change in place (his own data, not disposable test data) rather than reverting it.
- [x] Full verification suite: `npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav` — clean.

**Still not addressed** (separate, larger scope than this fix): the `'approved'`/`'assets_missing'` values in the `announcement_status` union are still never set by anything — only `'pending_review'` (initial) and now `'ready'` (via the new manual action) are reachable in practice. Whether those two states should mean something distinct (e.g. an actual asset-completeness check, or a genuinely separate human-approval step before "ready to generate") is a real design question for whenever Phase F's approval workflow gets properly scoped — this fix deliberately just unblocks the exact button that was stuck, using the exact value it already checked for, rather than guessing at a fuller state machine that isn't specified anywhere.

### Phase C v4.6 — Generation moved from a popup to a proper full-page workspace (2026-07-28)

Madhu's feedback after using the fixed Generate flow for real: clicking Generate gave no feedback for ~20 seconds, then dumped the result into a small popup; clicking outside the popup lost it, forcing a full regenerate just to look at it again. Explicit restructure request: move creative *generation* into the Stakeholder Announcement Engine's own main page as a real full-page workspace (variant *creation* already lives at `./admin`, per the 2026-07-27 split); Stakeholder Hub keeps owning stakeholder *data* (approve/reject, photo/logo, edit) but no longer does generation at all.

- [x] **Rebuilt `/admin/events/[id]/creative-templates` (base page)** from a lightweight link-out landing page into the actual generation workspace: Speaker/Partner sub-sections, a left-hand list of stakeholders **approved for announcement** (`announcement_status === 'ready'` — the exact gate the Stakeholder Hub's "Approve for Announcement" action, added in v4.5, already sets), and a right-hand full-page workspace for the selected stakeholder — no modal, nothing dismissible by an outside click.
- [x] **Existing announcements are fetched up front** (`GET /api/events/stakeholders/announcements?event_id=X`, already existed for the social calendar, reused as-is) and matched to stakeholders by `speaker_id`/`partner_id` — selecting someone who already has a generated creative shows it **immediately**, no regenerate required just to look at it again. This directly fixes "clicking outside makes it go away and user has to click generate again."
- [x] **Real inline loading state** replaces the ~20-second silent wait — a spinner plus "Generating creative and post copy — this can take up to 20 seconds…" renders exactly where the result will appear, using the exact same `generate`/`regenerate-creative`/`regenerate-copy` endpoints the old modal called (`AnnouncementReviewModal`'s logic ported in, not rebuilt).
- [x] **Real layout bug found and fixed during live verification**: an early version stretched the creative preview to fill the very wide `1fr` main column — on a real screen this made a 4:5-aspect-ratio box ~1250px tall, burying Post Copy and the regenerate controls far below the fold. Fixed with a 3-column layout (capped-width preview | post copy | assets used) instead of stacking everything under an oversized image — confirmed live afterward: everything fits in one viewport, no scrolling needed.
- [x] **Stripped generation entirely out of Stakeholder Hub**: removed `generateAnnouncement()`, the `reviewTarget`/`creativeVariants`/`variantChoice` state, the "Generate Announcement" button, and the now-fully-unused `AnnouncementReviewModal.tsx` (deleted). Replaced with a "Generate Creative →" link (shown once a stakeholder is `ready`) pointing to the new workspace, plus a reciprocal header link both ways between the two pages. "Approve for Announcement" stays in Stakeholder Hub — that's data approval, not creative generation.
- [x] Registry copy (`app/lib/registry/modules.tsx`) already described the base page correctly from the earlier split (written anticipating this move) — only a stale code comment referencing "the module's landing page" needed correcting to "the main workspace page."
- [x] **Full live verification**: approved-speaker list correctly includes only `ready` stakeholders (confirmed a `pending_review` speaker is excluded); selecting an already-generated speaker shows Post Copy and Regenerate Creative immediately with zero clicks; clicking Generate Again shows the real inline loading state; Admin Console link and the new Stakeholder Hub ↔ SAE header links both work; Stakeholder Hub confirmed to no longer show "Generate Announcement" anywhere. Full verification suite (`tsc`/`eslint`/`build`/`check:nav`) clean throughout.
- [x] **Deliberately not addressed this pass** (Madhu's own words: "a few other feedback... about how the creative is getting generated... we'll discuss that later, once this is moved") — this pass is the structural move only, not a content/quality pass on the generation itself.

### Phase C v4.7 — Clean Logo Base generator (2026-07-28)

Madhu specified an exact standard for a "clean logo": every partner/speaker logo, after background removal, gets placed onto a fixed 600×300px **solid white** canvas (corrected mid-discussion — earlier planning had assumed transparent, which was wrong) with defined safe-area padding — 40px on all sides normally, or an 80px-left/right "special case" for moderately-wide logos — so every logo looks consistently sized regardless of its native shape. Full design discussion (locked-in decisions, algorithm, format-support decisions) is in `~/.claude/plans/magical-singing-rossum.md`.

- [x] **Empirically corrected the special-case rule during implementation** — the verbal spec ("height > 30% of width triggers special case") looked like a blanket ratio check, but pixel-measuring all 6 of Madhu's real reference mockups (guide-line positions + logo bounding boxes, not just reading the description) proved it's conditional on orientation: the 30% threshold only applies to logos that would already be *width*-constrained (touching left/right) under normal padding. Height-constrained logos (even a perfect square or a very tall vertical wordmark, both measured well over 30%) never trigger it. Verified: all 6 samples predict correctly with the corrected rule, including the WIO special-case sample (measured 435×173 — matches Madhu's own stated "440×174" example almost exactly).
- [x] **`buildCleanLogoBase()`** (`app/lib/media/logo-engine.ts`) — alpha-keyed trim (real gotcha found and fixed: Sharp's default trim compares top-left-pixel RGB, which is noisy on a background-removed image since transparent pixels keep their original RGB under alpha=0 — fixed with an explicit `background: {r:0,g:0,b:0,alpha:0}` trim reference) → safe-area sizing per the corrected rule → `fit:'inside'` resize (same pattern already used in `composite.ts`) → composite onto solid white, `flatten()` + `removeAlpha()` (confirmed via a real test that `flatten()` alone does NOT drop the alpha channel — only `removeAlpha()` does; output is now genuinely 3-channel opaque, not just visually opaque). Verified against all 6 real shape samples via the actual implementation (not just the spike) — pixel-identical placement to Madhu's references.
- [x] **Real background-removal bug found and fixed**: the existing border-touching flood-fill could be tricked by a raw upload cropped tight to a logo's own deliberate colored plate (Lego's red background, Nivea's blue circle, Goldman Sachs' blue square — real examples Madhu gave) — confirmed via a real test that an unguarded run on a tightly-cropped Lego mark destroyed 54% of the logo (stripped the whole red plate, left the wordmark floating on transparent). Added `isNearNeutral()` guard: only proceed with removal if the sampled corner color is convincingly neutral (near-white or near-black, low channel spread). **A second bug found while building the guard itself**: the textbook HSL saturation formula (`(max-min)/(255-|max+min-255|)`) is numerically unstable near white/black — its denominator shrinks toward 0 in exactly that regime, so a faintly-tinted off-white corner `(246,254,255)` scored saturation=1.0 (the theoretical max), wrongly blocking removal on genuinely neutral backgrounds. Fixed by using raw channel spread (`max-min`) instead — numerically stable at any lightness. Re-verified after the fix: Lego/Goldman Sachs correctly protected (100% intact vs. unguarded), Nivea (genuinely neutral corner) still gets removal applied correctly.
- [x] **EPS support via CloudConvert** (`app/lib/media/cloudconvert-client.ts`) — confirmed via real research that no viable pure-JS EPS rasterizer exists (EPS is arbitrary PostScript, not a fixed structure like PDF/PSD; every real path needs Ghostscript, a system binary this Railway/Nixpacks deploy doesn't have). Checked whether the already-integrated Canva API could help first (it can't — confirmed via Canva's own docs: supported upload formats are JPG/PNG/SVG/GIF/PDF/PSD/AI/PPTX, no EPS). Madhu provided a real `CLOUDCONVERT_API_KEY` (added to `.env.local`, not Railway yet — needs his explicit go-ahead per the standing rule). Jobs API v2 (import/upload → convert → export/url, polled every 1.5s up to ~75s) — verified with a real hand-authored EPS file through both the direct client and the full `processLogo()` pipeline (real ~3s conversion), and again through the actual `POST .../upload-asset` route for a real speaker.
- [x] **Real extension-detection bug found via the route-level test**: both upload-asset routes (partners and speakers) determined the RAW file's stored extension by checking MIME type *before* the real filename extension — since `.ai` and `.eps` both commonly report `application/postscript`, every `.eps` raw upload was being stored with a `.ai` extension (confirmed live: an uploaded `test-logo.eps` landed in Storage as `company-logo-raw-....ai`). The actual conversion pipeline was unaffected (`detectLogoFormat()` in `logo-engine.ts` already correctly checks the real filename first) — this only mislabeled the raw file's stored name. Fixed in both routes: filename extension now checked first, MIME-type map only as fallback. Also added `eps`/`psd`/`psb` to both routes' allowed-extension lists (previously only checked in the format-detection layer, not the upload validation layer, meaning eps/psd uploads would have 400'd before ever reaching `processLogo()`).
- [x] **Fixed a real pre-existing gap**: speaker company-logo uploads always silently discarded the raw file's URL (uploaded to storage, never persisted to any DB column) — unlike the partner flow, which already persisted `logo_raw_url` correctly. Added `event_speakers.company_logo_raw_url` (applied live via `supabase db query --linked`, `supabase/sae_migration.sql` updated with history), speaker route now persists it.
- [x] **PSD support deliberately deferred** — `toRasterPng()` has a clear `psd` branch that throws "not yet supported" rather than a silent no-op. Per this codebase's established "prove it before building" discipline, wiring `ag-psd` needs a real spike against actual client PSD files first (the real risk: `ag-psd` only decodes Photoshop's own pre-baked embedded composite, present only when "Maximize Compatibility" was checked at save — not guaranteed for every file). Blocked on Madhu providing 2-3 real PSD files. **Resolved in Phase C v4.8 below.**
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean throughout. All test data (a disposable test partner, a test speaker's temporarily-overwritten company logo, and their Storage objects) cleaned up after verification — the real `CLOUDCONVERT_API_KEY` in `.env.local` is not test data, left in place.
- [x] **`CLOUDCONVERT_API_KEY` added to Railway** (2026-07-28) — Madhu gave explicit go-ahead. Set via `railway variables --set` on the `eventpilot` service, confirmed present (count-only check, value never echoed). EPS uploads now work in production, not just local dev.

### Phase C v4.8 — PSD support via `ag-psd` (2026-07-28)

Madhu provided 5 real client PSD files (`ifb.psd` 1000×500, `aventior.psd` 500×300, `e24.psd` 800×300, `manageengine.psd` 400×300, `zoho.psd` 300×300) to unblock the deferred spike from v4.7.

- [x] **Spiked `ag-psd`'s `useImageData` path against all 5 real files before wiring anything in** — the theoretical risk noted in the plan (no baked composite present unless "Maximize Compatibility" was checked) did not materialize on any of the 5: all decoded correctly via `psd.imageData` (raw RGBA), none needed to fall back to the `canvas` output mode. Visually diffed 4 of the 5 outputs against the source — pixel-correct, no corruption.
- [x] **Real gotcha found and fixed during the spike**: even with `useImageData: true`, `readPsd()` threw `"Canvas not initialized, use initializeCanvas method"` on every file — turned out `ag-psd`'s embedded-*thumbnail*-resource decoder (unrelated to the composite image data actually being read) unconditionally calls its internal `createCanvas`, regardless of the `useImageData` option. Fixed by calling `ag-psd`'s `initializeCanvas()` once at module load with `@napi-rs/canvas`'s `createCanvas` — the same canvas implementation already used for PDF/AI rasterization above it in this file, so no new native dependency (deliberately not `node-canvas`, consistent with this project's existing `next.config.js` `serverExternalPackages` choice).
- [x] **`rasterizePsdToPng()`** (`app/lib/media/logo-engine.ts`) — `readPsd(buffer, { useImageData: true, skipLayerImageData: true })` → `psd.imageData` (raw RGBA) → `sharp(..., { raw: { width, height, channels: 4 } }).png()`. Throws a clear, specific error (not a silent blank image) if `imageData` is ever absent on a future upload, rather than assuming the 5-file spike generalizes to every possible PSD.
- [x] Wired into `toRasterPng()`'s `psd` branch (previously a placeholder throw) — `PSD/PSB` is no longer deferred; `LogoSourceFormat`'s `psd` case is now fully live end-to-end through `processLogo()`.
- [x] **Verified through the real API route, not just the library** — logged in as super admin via the actual `/api/login` route (found the real `SUPER_ADMIN_PASSWORD` differs from the value documented in `CLAUDE.md`), uploaded a real `zoho.psd` through `POST /api/events/stakeholders/speakers/{id}/upload-asset` for a real (disposable-use) speaker, confirmed the response correctly returned both `company_logo_url` (600×300 processed PNG) and `company_logo_raw_url` (`.psd` extension preserved), downloaded and visually confirmed the processed output — correct trim/center/enlarge onto the solid white clean base, matching the direct-library spike result exactly.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean. Test data cleaned up: speaker's `company_logo_url`/`company_logo_raw_url` reset back to the original real value, orphaned Storage test objects removed.
- [x] New dependency: `ag-psd` (pure JS/TS, no native binary in the `useImageData` path — only needs a canvas factory for the unrelated thumbnail-resource branch, satisfied by the already-installed `@napi-rs/canvas`).

### Phase C v4.9 — Stakeholder Hub: photo upload UX, real delete + restore, bulk delete, live search (2026-07-28)

Real feedback from Madhu after hands-on use of the Hub: the old photo-upload flow gave no visible progress and auto-opened a crop tool that mostly wasn't useful; there was no genuine "delete" (only a silent one-click "Archive" with no confirmation and no way to see/undo it); no way to select and remove several stakeholders at once; no way to search the registry.

- [x] **`PhotoUploadModal.tsx`** (replaces `PhotoCropModal.tsx`, deleted) — opens the instant a file is picked, before the network call starts. Two real, observable stages via `XMLHttpRequest` upload-progress events (not faked with timers): "Uploading photo… N%" driven by `xhr.upload.onprogress` while bytes are still sending, then "Removing background & enhancing…" once `xhr.upload.onload` fires and the browser is waiting on PhotoRoom + Sharp server-side. Preview stage shows the processed photo with **Use This Photo** (primary), **Adjust Crop** (secondary, opens the crop tool inline in the same modal), and **Choose Different Photo** (re-upload without leaving the modal). X (top-right) is a real cancel — calls `xhr.abort()` if still mid-upload.
- [x] **Crop tool question answered, not just left alone**: Madhu asked whether the crop feature was even needed, since he "couldn't expand the selection width-wise" and always ended up choosing use-as-is. Root cause: the old default crop box was pre-inset 5% on every side (`{x:5,y:5,w:90,h:90}`), so an unadjusted "Confirm" silently cropped 5% off every edge, and there was barely any room to enlarge toward the image's own edges — expanding *beyond* a photo's own pixel bounds isn't possible for any cropping tool (there's no more image data past the edges to show), so this was partly an inherent limit, not a bug. Fixed the fixable part: default crop is now the full, untouched image (`0/0/100/100`), so "Use This Photo" and an unadjusted "Confirm Crop" now produce identical results, and Adjust Crop is a clearly secondary action rather than a forced step.
- [x] **Real "Delete" with typed confirmation** (`DeleteConfirmModal.tsx`) — reuses the "type DELETE to confirm" pattern already established in `app/admin/toolkit/knowledge-base/manage/page.tsx`. Investigated first: `event_speakers`/`event_sponsors` are the *same* tables read by the public event website, KonfHub sync, and site deploy (`app/api/public/event/[slug]/route.ts` et al.) — a true hard delete would silently remove a stakeholder from the live public site too. Flagged this to Madhu before building rather than guessing; his answer: **"Also remove from website" as an explicit opt-in checkbox** (unchecked by default — stays live on the public site, only hidden from the Hub) **+ a Deleted tab to restore from.**
- [x] Implementation reuses the existing `announcement_status='archived'` state (already correctly excluded from the default list query, already supports `?status=archived`) rather than a new column — Delete = the existing soft-delete, now with real UI. Two narrow, explicit opt-in additions to the speaker/partner `[id]/route.ts` PATCH/DELETE handlers: `also_remove_from_website` (DELETE body) sets `active:false`; `also_restore_to_website` (PATCH body) sets `active:true`. Every other caller of these routes (the normal edit-save flow) never sends these flags, so the pre-existing "never touch `active`" contract for those routes is otherwise unchanged.
- [x] **`DeletedTab.tsx`** — new nav entry below the category list, fetches both `?status=archived` speakers and partners together, **Restore** button per row (PATCH `announcement_status` back to `pending_review` + `also_restore_to_website: true`, so restore is a full, predictable undo regardless of whether "also remove from website" was checked at delete time).
- [x] **Bulk delete** — checkbox per row + "Select all", a red action bar appears once ≥1 selected ("N selected", "Delete Selected", "Clear selection"), routes through the same `DeleteConfirmModal` (count>1), fires one DELETE per selected id with the single chosen `also_remove_from_website` value.
- [x] **Live search** — client-side filter (data's already fully fetched) across every relevant field: speakers by name/job title/company/country/bio/LinkedIn URL, partners by company name/website/description/type. Case-insensitive substring match, updates as you type, no backend change.
- [x] **Verified live**, not just typecheck: real browser sessions via Playwright against the actual dev server (logged in through the real `/api/login` route) — upload → uploading-progress → processing → preview → Adjust Crop (confirmed the crop box now starts at the true full-image bounds) → Confirm Crop (confirmed a new processed file is persisted) → Choose Different Photo mid-modal reupload → X-cancel mid-upload, all confirmed working. Delete → Deleted tab → Restore → back in the main list, confirmed via UI. Bulk delete of 2 real rows, confirmed via UI. `also_remove_from_website`/`also_restore_to_website` flags verified directly against the database (first attempt used an unauthenticated `fetch` and silently no-opped via the auth middleware redirect — re-ran through an authenticated Playwright session and confirmed `active` flips both ways correctly). Live search verified against name/company/country, case-insensitive, and the zero-results case. All test speakers and their Storage objects deleted afterward.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean throughout.

### Phase C v5 — Per-field text boxes (name/title/company), real word-wrap, and a unified visual layer editor (2026-07-29/30)

Madhu reviewed a real generated creative and found speaker name/job title/company had no real layout container — a single-line SVG `<text>` at one baseline point, no wrapping, nothing stopping a long value from running off the edge. He asked to be consulted before any implementation given the scope; investigated the existing code first (confirmed `TextLayer` was genuinely point-only, confirmed there was no text-measurement/wrapping capability anywhere in `composite.ts`, confirmed zero drag/resize UI existed anywhere in the variant editor for ANY layer type) and came back with a plan before writing code. Locked in directly with Madhu: auto-shrink font size as the primary overflow strategy, floored at **60% of the configured size** then ellipsis-truncated; unify **all three layer types** (image/photo/text) under one visual box editor now, not text-only; existing live variants auto-migrate to sane default boxes rather than breaking.

- [x] **`TextLayer` gains a real box** (`app/lib/announcements/composite.ts`) — `width`/`height`/`max_lines` (default by field: name=3, title=2, company=2, tier=2, custom=2), `x`/`y` now mean box top-left (was an SVG baseline point) consistent with `ImageLayer`/`PhotoSlotLayer`. Dead `max_width` field (never actually used for wrapping) removed.
- [x] **`withTextLayerDefaults()`** deliberately split into its own dependency-free module (`app/lib/announcements/text-layer-defaults.ts`), not left inside `composite.ts` — `composite.ts` imports `sharp` + `@napi-rs/canvas` (native, server-only), and the variant editor (`'use client'`) needs to call this same defaulting function too, so it can't live anywhere that would pull native deps into the client bundle. Caught via a real `tsc`/build check, not assumed. Derives a plausible box from a legacy layer's old baseline point + `align` (best-effort, not guaranteed pixel-identical to the old single-line look — real production variants should be spot-checked). Called at exactly two sites: inside `compositeAnnouncement`'s text branch (the one true rendering call site — `preview/route.ts` forwards the draft variant straight through, so this single call covers live preview AND real generation), and at the editor's variant-load time purely for immediate visibility — that second call deliberately does NOT mark the variant dirty; only a real edit does.
- [x] **`wrapAndFit()`** (new `app/lib/announcements/text-layout.ts`) — real word-wrap + auto-shrink using `@napi-rs/canvas`'s `ctx.measureText()` (no DOM available server-side, Sharp/SVG has no auto-wrap of its own). Spike-tested standalone against real/edge-case strings *before* wiring into `composite.ts` at all (per this codebase's established discipline): a real long name wrapping cleanly, an unbreakable long token with no spaces, a long multi-word phrase forcing both shrink and truncation, empty/whitespace input, and the exactly-fits case. Found and fixed a real gap during spike testing: the first version only added an ellipsis when a single line was individually too wide, not when whole trailing lines got silently dropped by the `max_lines` cap — fixed so the last visible line always gets a forced ellipsis when content was truncated for either reason.
- [x] **Font measurement matches font rendering** — the same fetched font buffer is used both for the SVG `@font-face` embed and `@napi-rs/canvas`'s `GlobalFonts.register()` (mirroring the registration pattern already used in `app/lib/media/logo-engine.ts` for PDF rasterization), and the no-custom-font fallback uses the literal generic `'sans-serif'` on both the render and measurement side (not a comma fallback list like `'Arial, Helvetica, sans-serif'`) so wrap points can't silently diverge from what's actually drawn. Font buffers and `GlobalFonts` registrations are both cached process-wide (not just per-render) so the debounced live preview doesn't refetch/re-register on every keystroke.
- [x] **`analyzeTextLayers()`** — diagnostics-only companion to `compositeAnnouncement`, computed alongside the real render in `preview/route.ts` and returned as `text_diagnostics` per layer id. Surfaces an inline "shrunk to fit" / "⚠ truncated" badge next to the layer row in the editor, so Madhu doesn't have to notice a problem by squinting at the preview PNG.
- [x] **`LayerBoxOverlay.tsx`** (new) — hand-rolled drag/resize box editor over the live preview image, not `react-image-crop` (confirmed it's a single-box/single-image selector, the wrong shape for N simultaneous boxes over one shared preview — its one reuse was the displayed→natural pixel scale-factor math pattern already established in `PhotoUploadModal.tsx`). Pointer Events throughout (mouse/touch/pen unified from the start — no drag precedent existed anywhere in this app to extend). Box position/size expressed as CSS percentages of `canvas_width`/`canvas_height` (the preview container's aspect-ratio is already locked to match exactly, so no resize-observer bookkeeping needed); drag deltas converted screen-px→canvas-px via the container's live `getBoundingClientRect()`. 8 resize handles (corners + edges); dragging a top/left handle keeps the opposite edge anchored. Gesture-time dragging is clamped to stay on-canvas and above a minimum box size — free-typing the same values into the `NumField`s stays intentionally unclamped (off-canvas bleed is a legitimate design choice, just not a legitimate *drag accident*). All mutations flow through the pre-existing `updateLayer()` — zero new state plumbing, the existing debounced preview fires automatically.
- [x] **Required refactor, done rather than worked around**: `LayerRow`'s expanded/collapsed state was private per-row `useState` with no "which layer is active" concept in the parent — lifted to a single `expandedLayerId` on the page component so a click on a canvas box can open its accordion row and vice versa.
- [x] `ImageLayerFields`/`PhotoSlotLayerFields` needed no data-model change (already had real boxes) — they just gained the same overlay interaction; `ImageLayerFields` also gained X/Y/Width/Height `NumField`s it never had before (previously hard-locked to full-canvas with no manual override at all).
- [x] **Verified against real production data, not synthetic fixtures**: loaded the actual pre-existing "Speaker template 1" speaker variant (already generating live creatives) in the real editor — confirmed its legacy point-only name/title layers auto-migrated to sane boxes matching the defaulting formula exactly (e.g. width `Math.round(1080×0.4)=432`, height `Math.round(3×56×1.2)=202`), confirmed the migrated box visually aligned with where the name text actually renders in the composited preview, confirmed loading it did NOT mark it dirty (Save button read "Saved", not "Save Changes"). Selected a disposable real test speaker with a deliberately long name/title in Live Preview and confirmed real multi-line wrapping in the actual rendered PNG. Forced the shrink+truncate floor path with an artificially narrow box via a direct API call and confirmed both `did_shrink`/`did_truncate` diagnostics flipped true AND the rendered PNG showed a real ellipsis mid-word. Added a real disposable text layer through the UI, dragged its resize handle, confirmed the `NumField`s updated with correctly-scaled values, saved, reloaded the page, confirmed the new box size persisted — then deleted the test layer and re-saved, confirming the real variant round-tripped back to its original 7-layer shape exactly. All disposable test speakers deleted afterward.
- [x] Hit a real, external mid-session blocker unrelated to this code: this Supabase project's payment lapsed, causing widespread `TypeError: fetch failed` errors (DNS not resolving for the project host) and, after Madhu cleared payment, a stale PostgREST schema cache briefly returning `Could not find the table 'public.event_speakers'` until it caught up on its own — waited out via background connectivity monitors rather than guessing at a code-side workaround, confirmed recovery via direct `dig`/`curl` checks before resuming.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v5.1 — Speaker logo box sizing fix + head-detection caching (2026-07-30)

Two related fixes from the same real-creative review. First: the speaker company logo's `photo_slot` box (`241×50`, a 4.82:1 rectangle) didn't match the Clean Logo Base's fixed `600×300` (2:1) output, so most of the box rendered as empty space and only a tiny ~100×50 sliver of visible logo showed up, floating oddly disconnected from the rest of the card. Fixed directly in the "Speaker template 1" variant using the box-editor tooling from Phase C v5 (resized to `220×110`, repositioned inside the translucent info card) — this needed no code change, just confirms the Width/Height fields + drag-resize already apply to every `photo_slot` layer, logos included.

Second, a real bug Madhu caught live: after only touching the logo box and regenerating, the speaker's PHOTO crop had shifted too, despite never touching it. Investigated before assuming anything — confirmed via a direct DB read that the saved `speaker_photo` layer's box/alignment were byte-identical before and after, so the stored variant data wasn't the cause. Root cause found in `app/lib/media/face-alignment.ts`: `alignAndCropPhoto()` never cached where a speaker's head actually sits in their photo — every single generate/regenerate re-ran a fresh Gemini vision call (`detectHeadBox`) from scratch, and LLM-based detection isn't perfectly deterministic call-to-call, so the same unchanged photo could crop slightly differently between generations with nothing in the data or code actually changing.

- [x] **New `event_speakers.photo_head_box` column** (JSONB — `{ centerXRatio, centerYRatio, heightRatio }`, `detectHeadBox()`'s own return shape) — applied live via `supabase db query --linked`, verified present.
- [x] `alignAndCropPhoto()` gains an optional `cachedHeadBox` third parameter — `cachedHeadBox ?? await detectHeadBox(...)`, so a cached box skips the Gemini call entirely; `undefined`/`null` (legacy photos from before this column existed) falls back to the original live-detection behavior unchanged.
- [x] **Detected once, not on every render**: the speaker photo `upload-asset` route now calls `detectHeadBox()` on the final processed buffer right after upload and persists the result; the `crop-photo` route re-detects against the newly cropped buffer specifically (cropping changes the actual pixel content, so a carried-over cache would be wrong, not just stale) and overwrites the stored value. Both gracefully leave `photo_head_box` null on detection failure (no face found, API error) rather than blocking the upload/crop itself — generation falls back to live detection for that photo, same as before this change.
- [x] Threaded through every real call site: `ResolvedAssets` (`composite.ts`) gains an optional `head_box` per photo asset; `NeededAsset` (`app/lib/events/announcements.ts`) gains `headBox`, populated from `speaker.photo_head_box` inside `buildCompositeInputs()`; both `announcements/generate` and `announcements/[id]/regenerate-creative` attach it when building the assets record; `templates/preview` does the same so the live editor preview can never show a crop that a subsequent regenerate would then diverge from.
- [x] **Verified live, not assumed**: uploaded a real photo for a disposable test speaker, confirmed `photo_head_box` populated in the response; called the preview endpoint 3 times back-to-back for that speaker and confirmed all 3 renders hashed identically (SHA-256) — byte-for-byte reproducible, where before this fix the same test could plausibly have produced different crops each time. Cropped that speaker's photo and confirmed `photo_head_box` was recomputed (ratios changed, matching the new crop) rather than carried over stale. Called preview for a real, unmodified pre-existing speaker (`photo_head_box` still `null`) and confirmed the legacy live-detection fallback still returns a valid render, not a crash. All test data cleaned up afterward.
- [x] **Deliberate scope decision, consistent with this project's established pattern** (matches the Clean Logo Base backfill decision, Phase C v4.7): new uploads/crops only — existing speakers' photos keep `photo_head_box: null` and continue using live per-call detection (unchanged, not broken) until they're re-uploaded or re-cropped. No bulk backfill run.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6 — Variant maker UX overhaul: on-demand preview, render speed, editing ergonomics (2026-07-31)

Madhu found the variant editor's live preview unusable while adjusting boxes: every drag pixel triggered a full server-side Sharp render (several seconds), so editing felt janky. Root cause: `LayerBoxOverlay`'s drag already updated local state instantly (box position is pure CSS-percentage math, no network) — the lag was entirely a 500ms-debounced auto-render effect re-POSTing to `/api/events/templates/preview` on every state change. Discussed options; Madhu chose all four: on-demand preview button, stale-while-revalidate, real render-speed wins, and editing ergonomics (arrow-key nudge, snap guides, undo/redo) that plain didn't exist before.

- [x] **On-demand "Generate Preview" button** — deleted the debounced auto-render `useEffect` entirely. Dragging/typing now only ever touches local React state; the real Sharp render fires exclusively on the button click. Guarded against out-of-order responses with a request-id ref (a stale click's response can't clobber a newer one).
- [x] **Stale-while-revalidate** — clears the preview only when the box layout itself changed underneath it (`activeVariantId`/`activeType` change — an old image there would be actively misleading); switching which speaker/partner is injected (`previewFor`) keeps the last render visible, dimmed + badged "Stale — click Generate Preview," since the layout is still correct and only the injected content is stale. A separate "Generating…" badge overlays during a fresh render without blanking the image underneath.
- [x] **Render-speed wins**: (a) `compositeAnnouncement()`'s per-layer loop (`composite.ts`) and all three call-site asset-resolution loops (preview/generate/regenerate-creative routes) went from sequential `for await` to `Promise.all`-based parallel fetching — order-preserving (array index order survives regardless of completion order), verified this doesn't break z-order via a real multi-layer variant. (b) New shared `app/lib/announcements/asset-buffer-cache.ts` (mirrors the existing font-buffer-cache pattern), URL-keyed with zero invalidation logic needed since every upload route already mints a new timestamped URL on re-upload — a stale cache hit against old content is structurally impossible. (c) `preview/route.ts` downsamples its output to 50% before base64-encoding (smaller payload, faster transfer/decode) — `generate`/`regenerate-creative` stay untouched, always full-resolution.
- [x] **Undo/redo** — commit-point snapshots (`{speakerVariants, partnerVariants, activeType, activeVariantId}`), not per-keystroke/per-pointermove: pushed immediately for discrete actions (add/delete/move layer, add/delete variant), once at the start of an actual box drag (via a new `onCommitUndo` prop, only committed on the FIRST real pointermove of a drag so a no-op click doesn't pollute the stack), and on `NumField` blur only if the value actually changed from focus-time (discarding the snapshot on blur if unchanged). Global Cmd/Ctrl+Z / Cmd/Ctrl+Shift+Z plus toolbar buttons; guarded against firing while focus is in an editable element. Per Madhu's confirmed choice, undo restores both the variants AND `activeVariantId`/`activeType`, so it can jump the visible variant if the undone edit happened elsewhere — undo always shows you what it just changed.
- [x] **Arrow-key nudging** — the overlay container gets `tabIndex`/focus on box selection (not a window-level listener), so it never competes with typing elsewhere on the page by construction. 1px / 10px-with-Shift, clamped the same way drags already are. A burst of rapid nudges (holding the key) coalesces into one undo entry (new burst after 500ms of quiet).
- [x] **Snap guides** — canvas edges/center plus every sibling layer's edges/centers, ~6px canvas-space threshold, only the moving axis snaps (never silently resizes). Guide lines render as 1px teal divs while a snap is engaged during a move drag.
- [x] **Real bug found and fixed during a "React refs during render" ESLint check** (React 19's stricter `react-hooks/refs` rule): the initial "latest undo/redo function" ref pattern assigned `ref.current = fn` directly in the render body, which the new lint rule flags as unsafe — moved into a `useEffect` instead, the officially correct pattern.
- [x] **Verified live**, not assumed: confirmed editing a field does NOT auto-render (waited well past the old 500ms debounce window with nothing firing); confirmed Generate Preview produces a real render; confirmed the stale badge appears and the old image stays visible on a speaker switch; confirmed a render-speed improvement on a cache-warm second call for a speaker with a cached photo head-box (~2.0s → ~1.2s) with byte-identical (SHA-256) output between calls — separately confirmed via a speaker WITHOUT a cached head-box that the pre-existing (unrelated, already-documented) live-Gemini-detection non-determinism was the cause of an earlier confusing test result, not a bug in this pass's parallelization; confirmed undo/redo round-trips a field edit correctly; confirmed a 2-nudge burst (1px + Shift+10px) reverts as exactly one undo step; confirmed arrow keys inside a focused `NumField` move the text cursor, not the layer; confirmed a drag toward the canvas corner snapped to an exact `(0,0)`. All test data cleaned up afterward; no variant edits were ever saved during testing (no test script clicked Save Changes), so the real "Speaker template 1" variant was untouched throughout.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.1 — Custom brand fonts were silently never rendering (2026-07-31)

Madhu selected a real, correctly-uploaded custom font (Space Grotesk, Bold) for a text layer and the preview kept showing a generic default font instead. Investigated before assuming a caching bug (the obvious first suspect, given last session's font-buffer caching work): confirmed the saved variant JSON correctly referenced the right `font_family`/`font_weight`, confirmed the font file itself was valid (real `.woff2`, correct magic bytes) and correctly stored/reachable in Storage. The real cause was one level deeper — `sharp.versions.rsvg` reports `2.62.90`; **librsvg (the SVG engine Sharp uses to rasterize the text layer's SVG) does not reliably apply embedded base64 `@font-face` fonts.** Proved this directly: an SVG with a real Space Grotesk `@font-face` embed and an SVG with no font specified at all rendered to byte-for-byte identical PNGs — the custom font was being silently ignored on every single render, not just this one, meaning **no custom brand font had ever actually applied to a generated creative** (a real bug in Phase C v3's original text-rendering approach, not something introduced recently).

- [x] **Text rendering switched from SVG+Sharp/librsvg to direct `@napi-rs/canvas` rendering** (`renderTextLayerPng()`, replacing `buildTextLayerSvg()` in `composite.ts`) — draws the wrapped lines directly via `ctx.fillText()` onto a canvas the size of the full creative, using the exact same `GlobalFonts.register()`-based font (already proven correct, since `wrapAndFit()`'s own text-wrap *measurement* has used this same engine since Phase C v5 and was never the broken half). Rendering and measurement now go through one engine instead of two, which also removes a latent (never-hit-in-practice, but real) risk that the two engines could resolve a font differently even when nothing was visibly broken.
- [x] Dead code removed: `buildFontFaceCss()`, `FONT_FORMATS`, `fontFormatFromUrl()`, `escapeXml()` — all existed purely to build the SVG `@font-face` embed and safely interpolate text into XML, neither of which apply to a canvas `fillText()` call.
- [x] **Verified live, escalating from isolated to real**: (1) direct Sharp/librsvg test — an embedded-font SVG and a no-font SVG produced identical PNG bytes, confirming the root cause precisely; (2) direct `@napi-rs/canvas` test with the real downloaded Space Grotesk Bold file — produced a visually distinct, genuinely different render from the fallback font (different byte size, different pixels, real Space Grotesk letterforms visible); (3) full end-to-end test through the actual `POST /api/events/templates/preview` route against the real "Speaker template 1" variant and its real saved Space Grotesk Bold `font_family` — the speaker's name now visibly renders in Space Grotesk, not the generic fallback.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.2 — Client-side ghost overlay for live layer positioning (2026-07-31)

Madhu proposed a "per-layer locked preview" design where clicking Generate Preview on one layer would freeze just that layer's rendered content, leaving other layers untouched by subsequent edits. Explained the technical blocker before building anything: `compositeAnnouncement()` produces one flattened Sharp composite, not independently addressable/freezable layers, so there's no way to regenerate one layer's pixels in isolation without re-rendering the whole stack — and a full render is exactly the several-seconds cost v6 just eliminated. Proposed an alternative instead: render the *actual* content (real text in the real custom font, real photo/logo image) as a client-side HTML/CSS overlay directly on the drag box, using the browser's own font/image rendering (which — unlike librsvg, per v6.1 — handles `@font-face` correctly) instead of asking the server to render anything. This gives true live positioning feedback with zero server round-trip, while the expensive real Sharp render stays reserved for the explicit Generate Preview action. Madhu approved this direction.

- [x] `page.tsx`: `StakeholderOption` extended with real content fields (`job_title`, `company_name`, `photo_url`, `company_logo_url`, `logo_url`) and `StakeholderKind`/`StakeholderOption` exported; `fetchAll()` now captures these from the speakers/partners API responses instead of just `id`/`label`. New `previewForRecord` resolves the currently-selected preview stakeholder's full record, passed into `LayerBoxOverlay` alongside `activeType`.
- [x] `LayerBoxOverlay.tsx`: `resolveGhostText()`/`resolveGhostImageUrl()` map a layer's `field`/`source` to the real selected stakeholder's actual name/title/company/photo/logo (falling back to placeholder text only when no stakeholder is selected or the field is a static value like `tier`/`custom`). New `FontFaceStyles` component injects real `@font-face` CSS rules (deduped by family) for every text layer's configured font, sourced straight from the branding font URLs already on the layer — real browser font rendering, not a server rasterizer. Ghost text is font-size-scaled via CSS container query units (`cqw`, `containerType: inline-size` on the ancestor) so it stays resolution-independent as the canvas scales; ghost images are absolutely positioned with `object-fit: cover` for photo slots and `contain` for logos. Only rendered for the currently-active (selected) layer, on top of the drag box.
- [x] **Verified live** via Playwright against the real "Speaker template 1" variant with a real selected speaker: selecting the name text layer showed the real speaker's name in real Space Grotesk Bold, positioned exactly on the box, with no server preview call made (the "No preview yet — click Generate Preview" state underneath was untouched); selecting the speaker photo layer showed the real photo correctly positioned/cropped to the box; selecting the speaker logo layer showed the real logo correctly positioned/contained. Confirmed via screenshots.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.3 — Ghost overlay duplicated real text; Bold weight was silently never applying (2026-07-31, same day)

Madhu regenerated a preview right after v6.2 shipped and caught two real bugs live: the speaker name showed as a visible doubled/duplicate layer, and Bold (Space Grotesk) still didn't look right. Investigated both before assuming either was the same root cause as v6.1's librsvg fix — they weren't.

**Duplicate text — a genuine v6.2 regression.** The ghost overlay (real text/photo/logo drawn client-side over the active box) was rendering unconditionally whenever a layer was selected, with no awareness of whether a real, up-to-date server preview was already showing underneath — so after Generate Preview, the ghost's browser-rendered text sat directly on top of the real Sharp-rendered text at a slightly different position/size (different wrap engines), reading as a doubled layer.
- [x] `LayerBoxOverlay.tsx` gained a `showGhost` prop; ghost text/image only render when there's no real preview yet or the existing one is stale (`showGhost={!previewDataUrl || previewStale}`, computed in `page.tsx` and passed down) — once a fresh real render is showing, the approximation gets out of its way.

**Bold weight silently not applying — a real, deeper, pre-existing bug, NOT a v6.1 regression.** Traced empirically (isolated `@napi-rs/canvas` scripts before touching app code): Google Fonts' css2 API now serves almost its entire catalog — confirmed for both Space Grotesk and, as a second data point, Open Sans — as variable fonts, returning the literal SAME physical file for both the 400 and 700 weight blocks in the CSS response (real browsers pick the right instance out of that one file via the font's own variable weight axis; that's expected, documented Google Fonts behavior, not a bug on their end). `@napi-rs/canvas` has no variable-font-axis support: registering that single file and requesting `ctx.font = 'bold ...'` renders pixel-for-pixel identical to a non-bold request — confirmed directly, byte-for-byte — with no error and no fallback, just silently the wrong weight. This affects the entire custom-font feature for any Google Fonts-sourced family requesting Bold, not just Space Grotesk; v6.1's own verification never caught it because that test only checked whether the custom FAMILY applied at all (vs. the generic fallback), not whether BOLD specifically differed from regular within that family.
- [x] `composite.ts`: `ensureFontRegisteredForMeasurement()` now downloads both `regular_url`/`bold_url` and only trusts a font as having a genuine separate bold face when the two buffers are actually byte-different (`Buffer.equals()`) — true for hand-uploaded TTFs via the bulk-upload route (genuinely distinct files), false for most Google Fonts today. `renderTextLayerPng()` draws synthetic/faux bold itself (stroke-then-fill, ~4% of font size line width — the same "embolden by a fraction of an em" technique browsers used pre-variable-fonts) whenever Bold is requested but no true bold face is available, instead of silently rendering Regular. `wrapAndFit()`'s measurement pass correctly uses `'normal'` in that case too (matches what's actually drawn).
- [x] **Verified live**: isolated test confirmed requesting `bold` against a single registered Space Grotesk face rendered byte-identical to `normal` (proving the silent-failure, not a hunch); confirmed the new synthetic-bold stroke technique produces a visibly, genuinely bolder render of real Space Grotesk; full end-to-end retest through the real "Speaker template 1" variant and Alistair's real name showed a single (non-duplicated) name layer, visibly bolder than before. No test data was saved — no test script clicked Save Changes.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.
- [ ] **Follow-up not in scope here**: synthetic bold is a visual approximation (stroke-widened Regular glyphs), not real Bold-weight glyph data — won't be pixel-identical to Canva's own bold rendering. A pixel-perfect fix would need to statically instantiate the variable font's `wght` axis at 700 server-side (e.g. via a font-instancing library) rather than relying on `@napi-rs/canvas` to do it — worth a proper look if exact bold fidelity turns out to matter for real output, not just a “does it look bold” check.

### Phase C v6.4 — Field edits (font/weight/color/size/align) now reactivate the live ghost overlay (2026-07-31, same day)

Madhu asked, after a preview is generated, why changing a text layer's font/weight/etc. still required a full server re-render just to see the effect — the ghost overlay already renders real text in the real font/weight/color/size straight off live layer state, so this should be free. Confirmed: the ghost span's styles already read `font_family`/`font_weight`/`font_color`/`font_size`/`align` directly off the layer object, so it was already capable of reflecting any field edit instantly — the actual gap was `showGhost`'s gating: it only re-armed on a box *drag*, so a plain field edit (a `Select`/`NumField` change, no pointer gesture involved) left the ghost hidden and the old, now-inaccurate real render sitting there unchanged with no stale indicator at all.

- [x] `page.tsx`: `updateActiveVariant()` — the single choke point every layer/variant mutation already funnels through (field edits, drag, add/delete/reorder layer, canvas resize) — now marks `previewStale = true` whenever a preview exists, the same "keep the last render, dimmed + badged" treatment `previewFor` switches already got. This one change reactivates the ghost overlay for the active layer on any edit, for free, since the ghost's rendering was already wired to live layer state.
- [x] **Verified live** via Playwright against the real "Speaker template 1" variant: generated a real preview (no stale badge), then edited the name layer's Font size via its NumField — stale badge appeared immediately and the ghost text reappeared at the new size with zero server call; separately confirmed switching the Weight `Select` from Bold to Normal also immediately re-armed the stale badge and visibly thinned the ghost text. No test data saved.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.5 — Ghost re-arming created its own doubled-text look; text was vertically centered, hiding the box's real top edge (2026-07-31, same day)

Two more real issues Madhu caught immediately after v6.4 shipped.

**Ghost re-arming (v6.4) created a NEW doubled-text look of its own.** Editing a field now correctly reactivates the ghost — but the underlying stale real render (dimmed via opacity, not hidden) still has ITS OWN old text for that same layer sitting right there, just faded — so the crisp new ghost text now draws directly on top of the faded old text at a slightly different size/position, reading as a doubled layer again, just fainter than the original v6.2 bug.
- [x] `LayerBoxOverlay.tsx`: when the ghost is drawing for a layer AND a real preview exists underneath (`hasUnderlyingPreview`, threaded down from `page.tsx` as `!!previewDataUrl`), a `backdrop-filter: blur(8px)` + translucent scrim now renders first, sized exactly to that layer's box — masks out the stale render's own old content for that specific region before the crisp ghost draws on top, instead of leaving it legible underneath.
- [x] **Verified live**: reproduced the doubled look first (screenshot confirms it was real), then confirmed the mask fully resolves it — single crisp text, no ghosting. Separately verified against a `photo_slot` layer (mask applies there too) — the ghost photo itself renders fully crisp (confirmed via a direct isolated-element screenshot); an earlier full-page screenshot that looked soft/blurred was a Playwright full-page-capture timing artifact, not a real rendering bug — a viewport-clipped screenshot at `deviceScaleFactor: 1` showed it crisp.

**Text was vertically centered within its box, not top-anchored.** Madhu's own framing: seeing text sit in the middle of its box hid where the box's real top edge was, so the Y you set didn't visibly correspond to where the text actually started — most design tools (Figma, Canva, PowerPoint) anchor text boxes from the top by default, only centering if you explicitly ask for it. Both the real render and the ghost were consistently centering (not a ghost-vs-real mismatch, just the wrong default for both).
- [x] `composite.ts`'s `renderTextLayerPng()`: removed the `(layer.height - blockHeight) / 2` centering offset — text now starts right at `layer.y` (plus font ascent, so the cap-height sits just below Y as expected), growing downward.
- [x] `LayerBoxOverlay.tsx`'s ghost text container: `alignItems: 'center'` → `'flex-start'`, matching the real render exactly.
- [x] **Verified live**: real "Speaker template 1" name layer's rendered text now visibly starts at the box's top edge in both the ghost and a freshly generated real preview, not floating in the middle. **Flagged, not silently absorbed**: this changes the real vertical position of text in every EXISTING text layer across every already-configured variant, since box heights were set assuming center-alignment — worth a quick pass over other variants/templates to confirm nothing now looks off, before this is considered final.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.6 — Reusable "Placeholder data" content, editable and shared across every variant (2026-07-31, same day)

Madhu noticed "Jane Doe / Chief Officer / Acme Corp" looked hardcoded, and it was — duplicated independently in two places (`LayerBoxOverlay.tsx`'s ghost fallback and `preview/route.ts`'s `PLACEHOLDER_TEXT` + a flat gray box standing in for photo/logo), with no way to edit either and no placeholder image at all. Added one reusable profile per stakeholder type instead, stored alongside the variants on the same event so every variant naturally shares it.

- [x] `composite.ts`: new `PlaceholderProfile` type (`name`, `job_title`, `company_name`, `photo_url`, `company_logo_url`, `logo_url`); `CreativeTemplateConfig` gained a `placeholder?: { speaker?, partner? }` field — no migration needed, `events.creative_template_config` is already JSONB.
- [x] New `PUT /api/events/templates/placeholder` — targeted update of just `creative_template_config.placeholder.<type>`, mirrors `variants/route.ts`'s existing shape so it can never clobber the variants or the other stakeholder type's profile.
- [x] New `POST /api/events/templates/placeholder-upload` — deliberately a SEPARATE route from `/api/events/templates/upload` (layer assets), not a relaxed version of it: that route is intentionally PNG-only since layer background/overlay art needs a real alpha channel for Sharp's compositing to be correct, and loosening it risks silently breaking that. This route accepts jpeg/png/webp/svg instead, matching what the real speaker photo/logo upload routes already allow, since a placeholder photo is commonly a JPEG headshot.
- [x] `preview/route.ts`: fetches the event's `creative_template_config` (now via a new `event_id` field the client sends) and falls back through THREE tiers per field — real selected stakeholder → saved placeholder profile → the original hardcoded text/gray-box — for both text (`name`/`title`/`company`) and photo/logo assets. Placeholder photos go through the same `alignAndCropPhoto()` pipeline as real ones (live face-detection, no cached `head_box`, same as any legacy speaker photo).
- [x] `page.tsx`: new `PlaceholderPanel` component (Name/Job Title/Company fields + photo/logo upload for speaker; Company Name + logo upload for partner), toggled via an "Edit Placeholder" button next to the preview's stakeholder picker; `fetchAll()` loads the saved profile, `savePlaceholder()` persists it.
- [x] `LayerBoxOverlay.tsx`: `resolveGhostText`/`resolveGhostImageUrl` extended with the same three-tier fallback chain as the server route, so the ghost matches "Generate Preview" exactly regardless of which tier is actually in effect.
- [x] **Verified live end-to-end** against the real event: opened the panel, filled Name/Job Title/Company, uploaded a real test photo, saved — confirmed via a fresh `GET` that it persisted; confirmed the ghost overlay immediately showed the new name instead of "Jane Doe"; confirmed a freshly generated REAL preview (Placeholder data selected, no real speaker chosen) rendered both the uploaded photo and the new name, not the flat gray box/hardcoded text. Test placeholder profile reset back to `{}` afterward so the live event isn't left with test data.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.7 — Placeholder photo/logo upload was solving a problem that doesn't exist; simplified to text-only (2026-07-31, same day)

Before shipping v6.6, walked Madhu through how the real speaker/logo pipeline processes uploads (PhotoRoom background removal, Logo Engine cleanup, cached face-detection) to flag that the new placeholder-photo upload skipped all of that. Madhu's correction: it doesn't need to exist at all — when a designer creates a photo_slot/logo layer for a new variant, they already upload a reference layer showing a placeholder photo/logo fully positioned (`derive-alignment/route.ts`), which is what supplies the box + face-alignment target. A separately-stored placeholder photo/logo would just be redundant content nobody asked for. Only the placeholder TEXT (name/title/company) was ever actually needed, since text has no equivalent "designer already supplied it" mechanism.

- [x] `composite.ts`: `PlaceholderProfile` reduced to `{ name?, job_title?, company_name? }` — dropped `photo_url`/`company_logo_url`/`logo_url` entirely.
- [x] Deleted `app/api/events/templates/placeholder-upload/route.ts` outright (not deprecated/left dead — confirmed unused anywhere else first).
- [x] `preview/route.ts`: asset resolution reverted to its original two tiers (real stakeholder → flat gray box) — no placeholder-profile tier for photo/logo. Text resolution keeps its three tiers (real → placeholder profile → hardcoded) from v6.6, unchanged.
- [x] `LayerBoxOverlay.tsx`: `resolveGhostImageUrl` reverted to its pre-v6.6 shape (real record only, no placeholder fallback) — `resolveGhostText`'s placeholder-profile fallback stays.
- [x] `page.tsx`'s `PlaceholderPanel`: upload UI, upload state, and the `eventId` prop it only needed for uploads all removed — just the three text fields + Save.
- [x] **Verified live**: confirmed no upload controls render in the simplified panel; ran a clean, controlled sequence (reset via API → fresh `GET` confirms empty → fresh page load → read the actual rendered input values, not just a screenshot) to prove the fields genuinely render empty against an empty profile — an earlier screenshot mid-edit had briefly shown stale data, traced to a Next.js dev-server HMR timing artifact from editing files while a Playwright test was running concurrently, not a real bug (a fully clean re-run confirmed correct behavior). Test data reset to `{}` afterward.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.8 — The v6.7 correction was itself wrong: "Upload Reference Layer" never actually persisted the reference image (2026-07-31, same day)

Madhu tested the real workflow the v6.7 decision was based on — uploaded a real cleaned headshot via "Upload Reference Layer (auto-position)" on the "speaker photo" Photo/Logo Slot layer exactly as intended, regenerated the Placeholder-data preview — and it still showed the flat gray box. Traced it precisely: `PhotoSlotLayer` (`composite.ts`) never had a field to store an uploaded reference image at all. `derive-alignment/route.ts` only ever analyzed the uploaded buffer in-memory to derive `x/y/width/height` + face-alignment, then discarded it — nothing about the actual image has EVER persisted anywhere, in any version of this feature. This was true before today's changes too; it just never surfaced because nobody had tried previewing with Placeholder data selected against a freshly-derived photo_slot layer before. The button's own help text ("upload a transparent PNG showing a **dummy photo already correctly positioned**") reads as if that dummy photo becomes visible somewhere, which is exactly what led both Madhu and Claude to (wrongly) conclude in v6.7 that a separate placeholder-photo upload would be redundant with this flow. It wasn't redundant — it was solving a real gap the reference-layer flow never actually closed.

- [x] `composite.ts`: `PhotoSlotLayer` gains `reference_url?: string` — the uploaded reference image's URL, now persisted instead of discarded. Applies uniformly to all three `source` values (photo and both logo kinds share the same upload flow).
- [x] `derive-alignment/route.ts`: now also uploads the buffer to storage (`uploadPublicAsset`, same pattern as the existing layer-asset upload route) alongside deriving the alignment target, running both in parallel; returns `reference_url` in the response. Gained a required `event_id` field for the storage path.
- [x] `page.tsx`: `analyzeReferenceLayer()` saves the returned `reference_url` onto the layer alongside the existing box/alignment fields; `eventId` threaded down through `LayerRow` → `PhotoSlotLayerFields` for the upload call. Added a "Reference image saved ✓" confirmation line next to the existing "Face-aligned ✓" one, and updated the button's help text to accurately describe what now actually happens.
- [x] `preview/route.ts`: asset resolution gained a middle tier — real stakeholder → **this layer's own `reference_url`** → flat gray box. Deliberately per-layer (not per-stakeholder-type like the removed `PlaceholderProfile.photo_url` was) since different photo_slot layers across different variants can each carry their own independently-uploaded reference image, matching how each already gets its own independently-derived box/alignment.
- [x] `LayerBoxOverlay.tsx`: `resolveGhostImageUrl` gained the same middle tier, reading `layer.reference_url` directly (already in scope, no new prop needed).
- [x] `PlaceholderProfile` (name/job_title/company_name) is unaffected and unchanged — that part of the v6.7 decision was correct; only the photo/logo half needed reopening.
- [x] **Verified live** with a disposable test variant (not the real "Speaker template 1" — the plan called for this given how visible last session's Alistair-related testing already was): added a fresh Photo/Logo Slot layer, uploaded a real reference photo via "Upload Reference Layer," confirmed the "Reference image saved ✓" indicator appeared, generated a preview with Placeholder data selected — the real reference photo rendered correctly (cropped into the box), not a gray box. The test variant was never saved (no "Save Changes" click), confirmed via a direct DB read that it never persisted — no cleanup needed.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v6.9 — reference_url was the full mockup canvas, not the trimmed content; live-detection non-determinism carried straight through (2026-08-01)

Madhu tested the v6.8 fix for real: speaker photo preview worked, but uploading a logo reference and regenerating showed no logo at all, and the speaker photo came back slightly misaligned. After a page refresh (losing the in-memory preview — expected, client-only state) and a fresh Generate Preview, the photo was noticeably MORE distorted than before, not the same.

Traced precisely: `reference_url` was being uploaded as the RAW file — the full reference canvas (e.g. 1080×1350, the whole variant's dimensions), with the actual photo/logo occupying only a small sub-region and the rest transparent, exactly as `deriveAlignmentTarget()`'s own alpha-trim box detection already knew (that's literally what `box` is) — but the trimmed pixels themselves were computed and discarded, never what got uploaded. Two distinct failure modes from the same root cause: (1) for the logo, resizing the WHOLE mostly-empty canvas down to fit a 241×50 box shrank the actual logo content to invisibility; (2) for the photo, `alignAndCropPhoto()` expects a buffer shaped like an actual photo, not a huge mostly-transparent canvas — feeding it the full canvas meant its own internal live face-detection call had to search a huge near-empty image, non-deterministically, producing a different (and here, worse) crop on each regenerate — the exact bug class `photo_head_box` caching solved for real speakers (Phase C v5.1), just never extended to reference photos.

- [x] `derive-alignment/route.ts`: uploads the TRIMMED buffer (`sharp(buffer).trim()`) as `reference_url`, not the raw upload — the same trim `deriveAlignmentTarget()` already performs internally, just also kept instead of discarded. Also runs `detectHeadBox()` once against that trimmed buffer and returns it as `reference_head_box`, caching the detection exactly like the real-speaker-photo upload flow does.
- [x] `composite.ts`: `PhotoSlotLayer` gains `reference_head_box?: HeadBox | null`.
- [x] `page.tsx`: `analyzeReferenceLayer()` saves `reference_head_box` onto the layer too (photo only — logos never go through `alignAndCropPhoto`).
- [x] `preview/route.ts`: the `reference_url` fallback tier now passes `layer.reference_head_box` as the cached head box, the same way `speaker.photo_head_box` already is for real speakers.
- [x] **Verified live** with a disposable test variant, reproducing the exact bug with the real full-canvas reference files Madhu used: uploaded a photo reference AND a logo reference, generated a preview — the photo rendered correctly cropped (no distortion) and the logo was now visibly present (previously invisible). Generated a second preview back-to-back and confirmed byte-for-byte identical output (SHA-256 match) between the two, confirming the non-determinism is gone. Test variant was never saved (no Save Changes click), confirmed via direct DB read — no cleanup needed.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.
- [ ] **Follow-up flagged, not built**: Madhu separately noted that a generated preview disappears on a full page refresh ("once a preview is generated, that version of preview should remain and not go away"). This is expected, by-design behavior — `previewDataUrl` has only ever lived in client-side React state since the whole on-demand-preview architecture shipped (v6), never persisted anywhere — not a regression from this fix. Worth a real product decision (e.g. cache the last preview per variant in `sessionStorage`) if Madhu wants it, but out of scope here since it wasn't the bug being chased.

### Phase C v7.0 — Logo boxes now resize aspect-locked to the Clean Logo Base's 2:1 ratio (2026-08-01)

Madhu's framing: a real speaker/partner logo always gets standardized onto the Clean Logo Base's fixed 600×300 (2:1) canvas — that's the whole point of the existing Clean Logo Base generator — so a template's logo box built against an off-ratio reference (like the real "speaker logo" box, 241×50 ≈ 4.82:1) would end up cropping or letterboxing a real, standards-compliant logo once it's swapped in for a real speaker/partner. Asked for logo boxes to resize aspect-locked going forward, and for the existing off-ratio box to be corrected too.

- [x] `LayerBoxOverlay.tsx`: drag-resize now locks width:height to exactly 2:1 for any `photo_slot` layer sourced from `speaker_logo`/`partner_logo` (not `speaker_photo`, whose sizing is governed by face-alignment, not a fixed ratio). Corner handles drive off width, deriving height; pure edge handles (`n`/`s`/`e`/`w`) drive off whichever axis is being dragged and re-center the other axis, since there's no drag intent to anchor an edge on there. The existing on-canvas clamp still applies on top, so a re-centered box correctly gets pulled back on-canvas rather than allowed to go negative.
- [x] **Real bug caught and fixed during this same change**: initially exported the `2` ratio constant from `composite.ts` and imported it into `LayerBoxOverlay.tsx` (a client component) — broke the entire client bundle (`Module not found: Can't resolve 'child_process'`, from `sharp`'s `detect-libc` dependency), since `composite.ts` imports `sharp` at its top and ANY runtime (non-type) import from it drags that whole server-only module graph into the browser. Fixed by duplicating the plain number as a local constant directly in `LayerBoxOverlay.tsx` instead — the file already imports everything else from `composite.ts` as `import type`, which erases cleanly at compile time; a real value export does not.
- [x] **Verified live**, both the fix above (confirmed the dev server's client bundle recovered, `localhost:3003` returned 200 again) and the actual ratio-lock behavior via real pointer drags on a disposable test variant: dragging the east (pure horizontal) handle, the southeast corner handle, and the south (pure vertical) handle each landed on exactly 2:1 afterward (e.g. 200×100 → 402×201 after an east-handle drag; a south-handle drag correctly re-centered horizontally and then got clamped on-canvas when centering would have gone negative — confirmed as the existing, correct clamp behavior, not a new bug).
- [x] **Snapped the real "speaker logo" box** in "Speaker template 1" from 241×50 (4.82:1) to 100×50 (2:1) — kept height, shrank width, recentered (`x` 739→810), done through the actual editor UI (NumFields + Save Changes) like any other real-data edit this session, not a direct DB script. Confirmed persisted via a direct read afterward.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v7.1 — Aspect lock only covered drag-resize, not the "Upload Reference Layer" path that also sets a logo box (2026-08-01, same day)

Madhu caught this immediately: uploaded a real logo reference layer to the (already-snapped, 100×50) "speaker logo" slot, and the box reverted to an off-ratio 241×50 again. Real gap in v7.0 — the aspect lock was only implemented inside `LayerBoxOverlay.tsx`'s pointer-drag handler, but a logo box's dimensions are also set through a completely separate path: "Upload Reference Layer (auto-position)" sets `x/y/width/height` directly from the uploaded PNG's own raw alpha-trim bounding box (`analyzeReferenceLayer()` in `page.tsx`), which never goes through a drag at all and so never hit the lock.

- [x] `page.tsx`: `PhotoSlotLayerFields`'s `analyzeReferenceLayer()` now snaps the derived box to 2:1 (shrinking around its own center, same math as the manual real-data snap) before applying it, for logo sources only — a photo box's derived box is untouched, since photo sizing is governed by face-alignment, not a fixed ratio. Duplicates the `2` ratio constant locally rather than importing it from `LayerBoxOverlay.tsx` or `composite.ts` — same reasoning as v7.0's client-bundle bug, avoided proactively this time.
- [x] **Verified live**: re-uploaded the exact same reference logo file Madhu used, against a disposable test variant, and confirmed the derived box now lands on exactly 100×50 (2:1) instead of the raw 241×50 alpha-trim bounding box. Confirmed the real "speaker logo" layer in "Speaker template 1" is unaffected by this test (still the correctly-snapped 100×50 from the v7.0 fix — Madhu's screenshot showing 241×50 was an unsaved in-browser state from before that fix, not a DB regression).
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v7.2 — Speaker photo was still distorting after the logo fix; a pre-existing reference predated head-box caching (2026-08-01, same day)

Madhu re-uploaded the logo (now correctly 2:1) and regenerated — the logo looked right, but the speaker photo distorted again, asking "why does this keep happening?" Checked the real data directly: the speaker photo's `reference_url` (`reference-1785573771255.png`) was uploaded on 2026-07-31, BEFORE `reference_head_box` caching existed at all (v6.9) — it had no cached head box, `reference_head_box` simply absent from the layer. Every single preview since had been falling all the way through to live, uncached Gemini detection against that same unchanged reference image, non-deterministically — the exact bug v6.9's caching was built to prevent, just never applied retroactively to data that predated the fix. Not a new bug or a regression from the logo work; a leftover gap in already-existing real data.

- [x] No code change — re-ran the existing "Upload Reference Layer" flow (now with caching) against the real "speaker photo" layer in "Speaker template 1," using the same reference file already on hand, through the actual editor UI, and clicked Save Changes. `reference_head_box` is now populated (`{ heightRatio: 0.5, centerXRatio: 0.5, centerYRatio: 0.325 }`).
- [x] **Verified live**: generated the real preview three times back-to-back and confirmed byte-for-byte identical output (SHA-256 match) across all three — the non-determinism is gone. Visually confirmed the photo renders correctly proportioned, no distortion.
- [x] **Note for future occurrences of "why does a reference photo keep shifting/distorting"**: check whether that specific layer's `reference_head_box` is actually populated in the saved data before assuming a code regression — any reference upload made before 2026-08-01 (when this caching was added) will be missing it and needs exactly this same one-time re-upload to backfill.

### Phase C v7.3 — Two independent Gemini detections of the same head didn't agree, deterministically cutting off the top of the head (2026-08-01, same day)

The v7.2 backfill fixed the non-determinism (byte-identical renders), but Madhu kept testing methodically — hard refresh, generate once, check; upload logo, regenerate, check; hard refresh again, regenerate again — and isolated something new: with the non-determinism gone, the photo now *consistently* showed the same result every time, but with the top of the head visibly cut off. Stable and deterministic, unlike anything seen before in this whole investigation — a real, different, and much more precisely diagnosable bug than the earlier "keeps changing" reports.

Root cause: `reference_head_box` was being computed via a SEPARATE, independent `detectHeadBox()` call from the one `deriveAlignmentTarget()` already runs internally to derive `target_head_center_x/y/height` (both against the same trimmed image, just two separate Gemini requests). Even though both calls are detecting the same head in the same picture, they don't reliably agree pixel-for-pixel — and `alignAndCropPhoto()`'s scale/position math amplifies any small disagreement between "where the alignment target says the head should end up" and "where the cached head-box says the head currently is" into a visibly wrong crop. Flagged this exact risk when the caching was first added ("an acceptable one-time cost on upload") but underestimated it — it's not a one-time cost, it's a permanent, deterministic misalignment baked into that upload's data forever.

- [x] `derive-alignment/route.ts`: `reference_head_box` is no longer a second `detectHeadBox()` call — built directly from `target`'s own `target_head_center_x/y/height` fields (the exact numbers `deriveAlignmentTarget()`'s single internal detection already produced). Since a reference layer's box already exactly matches its own trimmed pixel dimensions, using the identical numbers for both is mathematically a no-op crop — the result is just the reference image itself, matching what the ghost overlay's plain CSS already shows for it. One fewer Gemini call per upload, too.
- [x] **Verified live**: disposable test variant confirmed a full, uncut head in the resulting preview (previously cut off with the old two-call approach); re-ran the fix against the real "speaker photo" layer in "Speaker template 1" and confirmed `reference_head_box` now exactly mirrors `alignment`'s own values (`0.611/0.505/0.3895` on both, not two different numbers); regenerated the real preview three times back-to-back — byte-for-byte identical (SHA-256 match) each time, full head visible, no cutoff.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

### Phase C v7.4 — Preview now survives a page reload; Save button moved next to the preview it saves (2026-08-01)

Two asks: (1) `previewDataUrl` only ever lived in client React state since the on-demand-preview architecture shipped (v6) — a page reload always lost it, showing "No preview yet" even right after saving; Madhu wanted whatever was on screen at save time to still be there on a later visit. (2) The Save button lived in the page header, visually far from the Preview panel and its Generate Preview/Edit Placeholder buttons it's functionally tied to.

- [x] `composite.ts`: `Variant` gains `last_preview_url?: string` — a real Storage URL, not inline base64 (keeps `creative_template_config`'s JSONB blob from ballooning).
- [x] New `POST /api/events/templates/save-preview` — uploads a preview PNG to storage, returns its URL. Mirrors the existing `templates/upload` route's shape.
- [x] `page.tsx`'s `save()`: if the currently-displayed preview is a fresh, unsaved `data:` URL, uploads it via the new route first and swaps local state to the resulting real URL; if it's already a persisted URL (loaded from a previous save), passes it straight through. Either way, `last_preview_url` rides along in the same `variants` array already being saved — no extra request beyond the one upload. Whatever's on screen at save time is what's kept, stale or not — regenerating is cheap if it's out of date, and second-guessing which state to keep wasn't asked for.
- [x] The variant/type-switch effect that used to unconditionally clear the preview to `null` now restores `activeVariant.last_preview_url` instead — not marked stale, since it was generated against and saved alongside that exact layer state by construction; only a new edit after loading marks it stale again.
- [x] Save Changes button moved from `PageHeader`'s actions (top of page) into the Preview panel's own header row, next to Edit Placeholder/Generate Preview. Undo/Redo stayed in the page header — global page actions, not preview-specific.
- [x] **Verified live** with a disposable test variant: generated a preview, saved, did a full fresh page reload, selected the variant again — the preview showed immediately (a real `https://` URL, not `data:`), no "No preview yet," no stale badge. Confirmed the Save button now sits directly beside Generate Preview and is gone from the header. Test variant deleted and the deletion saved afterward — confirmed the real "Speaker template 1" untouched.
- [x] Full verification suite (`npx tsc --noEmit`, targeted `eslint`, `npm run build`, `npm run check:nav`) clean.

---

## Phase D — Approval Workflow

## Phase D — Approval Workflow ✅ committed, verified end-to-end

- [x] D1: `send-for-approval/route.ts` — `announcement_approvals` rows with `approval_token`/`token_expires_at` (7-day, mirrors `staff_members.reset_token`), Resend email via the `RESEND_FROM` pattern.
- [x] D2: `app/admin/events/[id]/announcements/[announcementId]/review/page.tsx` — note the path uses `[announcementId]`, not `[id]` as the PRD literally wrote it (`app/admin/events/[id]/announcements/[id]/review` reuses the same dynamic-segment name twice, which Next.js flatly disallows — real bug in the PRD's own file list, not a style choice). Reads `?token=` via the codebase's established `window.location.search` pattern (not `useSearchParams()`, which would need a Suspense boundary this codebase avoids elsewhere).
- [x] `review-data/route.ts` — dedicated read-only GET for the review page, not in the PRD's file list. Kept deliberately separate from the (still unbuilt) general announcement CRUD route so a token only ever unlocks exactly what an approver needs to see, not a wider surface.
- [x] D3: `approve/route.ts` — accepts `token` OR `approver_id`; status-aggregation (all approved/approved_with_comments → announcement approved; any changes_requested → changes_requested); MM notification email (to `stakeholder_announcements.created_by`).
- [x] `middleware.ts`: added regex exceptions (not simple prefixes — both routes have dynamic `[id]` segments) for `.../approve`, `.../review-data`, and the review page itself.
- [x] `AuthedShellGate.tsx`: extended with a small `REGEX_NO_SHELL` list (prefix/exact matching can't express a two-dynamic-segment path) so the review page never shows the internal admin chrome, for external approvers or staff alike.
- [x] `app/lib/registry/nav-exclusions.ts`: added the review page to `PAGEHEADER_EXEMPT` — it's a standalone layout like the public forms, just placed under `app/admin/**` by the PRD.
- [x] **Verified fully end-to-end against the live database**, not just typecheck: seeded a real test announcement + approval row, hit `review-data` unauthenticated (200, correct data), loaded the review page itself unauthenticated (200, not a redirect), POSTed a real approval decision unauthenticated (200, `announcement_status: "approved"`), confirmed both `stakeholder_announcements.status` and `announcement_approvals.status` updated correctly in the DB, then deleted all test rows.

**Note**: the PRD's own text (§6.9) mentions the token being "HMAC-signed with JWT_SECRET" — the actual implementation uses a random opaque token stored in the DB (mirroring `reset_token`), per the plan decision made before Phase A ("reuse the existing pattern... rather than pulling in JWT/signing libraries"). No `JWT_SECRET` env var needed.

---

## Phase E — Postiz Cloud Integration ✅ code committed, blocked on Postiz account for live testing

**PRD v1.3 correction (2026-07-21): self-hosted Postiz → Postiz Cloud (platform.postiz.com, Team plan $39/mo).** No Railway deployment, ever — this whole phase was never started, so there was no code to fix, only this checklist's now-obsolete deployment items (removed below). Postiz Cloud's Meta/Google apps are pre-approved, so Instagram/YouTube connect immediately — no Meta App Review wait.

- [x] `app/lib/postiz.ts` — shared client (`schedulePostizPost`/`getPostizPostStatus`), used by all three routes below.
- [x] E1: `app/api/events/stakeholders/announcements/[id]/schedule/route.ts` — validates `approved`/`approved_with_comments` status + a configured `postiz_profile_key` before calling Postiz; stores `postiz_post_id`, sets `status: 'scheduled'`.
- [x] E2: `app/api/events/stakeholders/announcements/[id]/publish-now/route.ts` — same validation, omits `date`. Deliberately still lands on `status: 'scheduled'` (with `scheduled_for = now`), not `'published'` directly — Postiz accepting the request confirms it was queued, not that it's actually live on every platform; the sync-status cron confirms real publication for both paths uniformly.
- [x] E3: `app/api/cron/announcements/sync-status/route.ts` — polls every `scheduled` announcement past its `scheduled_for` with a `postiz_post_id`, marks `published`/`failed`, emails the MM on failure. `Authorization: Bearer CRON_SECRET`, matching the existing cron convention.
- [x] E4: `app/api/events/stakeholders/announcements/route.ts` (list/calendar query, not in the PRD's file list but required by E4's UI — same gap pattern as the earlier `submissions` list route) + `CalendarView.tsx`, wired into the Stakeholder Hub as a Registry/Calendar toggle. Month grid, coloured dots (speaker=indigo, partner=amber), click for detail, next-available-day suggestion.

**Verification**: `npx tsc --noEmit`, `npm run build`, `npm run check:nav`, eslint all clean. Route-level auth guards (staff-only, not public) confirmed via curl (307 to `/login` unauthenticated, correct). Could not exercise the real Postiz call path or the schedule/publish-now success path — blocked on Madhu's Postiz Cloud account existing at all (see below). Validation-branch logic (`422` for wrong status / missing profile key) is simple and deterministic, verified by reading rather than live-testing.

**[MADHU — manual, in parallel with E1–E4]:**
- [ ] Sign up at platform.postiz.com, Team plan ($39/month)
- [ ] Generate a global API key (Settings → API Keys) → `POSTIZ_API_KEY`
- [ ] Create a "World AI Show Malaysia 2026" workspace, connect LinkedIn/X/Instagram/YouTube via OAuth (all connect immediately on the Cloud plan, no approval wait)
- [ ] Generate a workspace API key → paste into the event's "Postiz Profile Key" field in EventPilot
- [ ] Give Claude Code `POSTIZ_API_URL=https://api.postiz.com` and the global `POSTIZ_API_KEY` to add to `.env.local` + Railway (Railway env var changes still need your explicit go-ahead in the moment, same as the Canva credentials)

---

## Phase F — End-to-End

- [ ] F1: Announcement generator slide-over panel in Stakeholder Hub (PRD §9.5) — post copy editor, creative preview, approver selection, Skip Approval / Send for Approval actions
- [ ] F2: Wire generate → approve → schedule end-to-end
- [ ] F3: Create "World AI Show Malaysia 2026" sandbox event; seed 2–3 test speakers + 1–2 test partners; full manual click-through, one speaker + one partner, real Postiz publishing gated off until Madhu confirms social accounts are connected

---

## Before merging to `main`

- [ ] Fix the pre-existing lint debt in `app/admin/events/[id]/page.tsx` (flagged above, Phase A)
- [ ] Full `npm run typecheck` / `npm run lint:changed <base>` / `npm run check:nav` / `npm run build` pass on the whole branch
- [ ] Authenticated browser click-through of the complete flow (not just build/typecheck) — get working local credentials sorted first
- [ ] Confirm the existing Website Builder speaker/sponsor flow and the live `AI-2047-2026` public site are unaffected (re-check before/after)
- [ ] Madhu + Durga review before push (this is a platform-wide, multi-integration change — same discipline as the nav/branding CI work)

---

## Manual action items — full list (for Madhu)

1. ~~`PHOTOROOM_API_KEY`~~ — **done 2026-07-21.** Key handed over, set in `.env.local`. **Not yet on Railway** — needs explicit go-ahead before setting there.
2. ~~Verify `CANVA_CLIENT_ID`/`CANVA_CLIENT_SECRET`~~ — **done 2026-07-21.** Existing "Event Pilot" Canva Integration found, scopes extended (`brandtemplate:meta:read`, `brandtemplate:content:read`), credentials set in `.env.local` + Railway, staff OAuth connection verified live (`canva_tokens` row confirmed).
3. ~~Canva Brand Template Autofill~~ — **moot**, PRD v1.4 dropped Autofill entirely for Sharp compositing (see Phase C v2). Superseded by item 8 below (multi-layer creative editor) — Madhu is now looking at each source design's actual layer structure to inform that build, not hunting for a Canva data-field option that doesn't exist.
4. ~~Deploy/create Postiz admin account~~ — **not applicable**, PRD v1.3 moved to Postiz Cloud (platform.postiz.com), no self-hosting.
5. ~~Sign up at platform.postiz.com, generate `POSTIZ_API_KEY`~~ — **done 2026-07-21.** Global API key handed over, set in `.env.local`. **Not yet on Railway.** Per-event/workspace `postiz_profile_key` still pending — that's entered per-event in the EventPilot UI itself (event profile edit form), not an env var.
6. Connect LinkedIn/X/Instagram/YouTube in the Postiz Cloud workspace (all connect immediately on the Cloud plan — pre-approved Meta/Google apps, no review wait). **Still open.**
7. ~~Submit Meta App Review~~ — **not needed**, Postiz Cloud's own Meta app is pre-approved.
8. **New (2026-07-21): multi-layer creative editor.** Real WAIS Malaysia creatives (e.g. the "Speaking At" design) have several independently-positioned elements — background gradient, decorative headline text, a static event-branding block, a speaker photo with a feathered edge blending into the background, a CTA button, and a bottom info bar — not just "one background PNG + a photo zone + text fields." Madhu is reviewing the actual Canva layer structure of the source designs to scope this properly before it's built. See the architecture discussion below/in-session for the proposed design (named creative variants, each an ordered stack of layers — image/photo-slot/text — reorderable and positioned per layer, live preview). **Blocks Phase C3's real-template test** — the single-background `composite.ts` built so far proves the pipeline works but won't reproduce these real designs until this lands.

**Note on Railway billing (found + resolved 2026-07-21):** Railway auto-deploy had silently stopped since 2026-07-17 due to a payment lapse — every push to `main` since then (including this morning's CI/nav-branding work) sat undeployed until Madhu cleared the payment and Claude Code force-triggered a fresh deploy (`railway redeploy --from-source`). Worth a quick sanity check that nothing else was expected to be live from that window.
